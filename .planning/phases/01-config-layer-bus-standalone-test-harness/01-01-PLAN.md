---
phase: 01-config-layer-bus-standalone-test-harness
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.js
  - .gitignore
  - tests/config.test.js
  - tests/bus.test.js
  - tests/harness.test.js
  - tests/fixtures/test-emitter.js
  - tests/fixtures/test-subscriber.js
autonomous: true
requirements: [CFG-01, CFG-02, BUS-01, TEST-01]

must_haves:
  truths:
    - "`npx vitest run` executes and reports RED for config, bus, and harness suites (implementations not yet written)"
    - "The test suite mechanically encodes CFG-01, CFG-02, BUS-01, and the TEST-01 structural check as executable assertions"
    - "bus tests prove decoupling: test-emitter.js and test-subscriber.js each import only src/bus.js, never each other"
  artifacts:
    - package.json
    - vitest.config.js
    - tests/config.test.js
    - tests/bus.test.js
    - tests/harness.test.js
    - tests/fixtures/test-emitter.js
    - tests/fixtures/test-subscriber.js
  key_links:
    - "vitest.config.js sets environment: 'happy-dom' so CustomEvent/EventTarget/DOM parsing work in tests"
    - "package.json type: module enables plain ESM JSON imports consumed by Wave 1/2 source"
---

<objective>
Scaffold the greenfield repo's dev tooling and author the full failing (RED) test suite that mechanically encodes this phase's four requirements before any implementation exists.

Purpose: This is a TDD Wave-0 plan. It establishes the executable acceptance criteria (Nyquist compliance) so every downstream implementation plan turns RED tests GREEN rather than being verified by prose. No source modules exist yet — every test here is expected to fail until Wave 1/2 lands.
Output: package.json, vitest.config.js, .gitignore, and the config/bus/harness test files plus the two decoupled bus fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-config-layer-bus-standalone-test-harness/01-RESEARCH.md
@.planning/phases/01-config-layer-bus-standalone-test-harness/01-PATTERNS.md
@.planning/phases/01-config-layer-bus-standalone-test-harness/01-VALIDATION.md
@CONTRACT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize package.json, dev dependencies, and Vitest config</name>
  <read_first>
    - 01-RESEARCH.md "Standard Stack" (lines 48-81) — exact pinned versions and the happy-dom-over-jsdom rationale
    - 01-VALIDATION.md "Test Infrastructure" + "Wave 0 Requirements" (lines 17-24, 51-58)
    - CLAUDE.md hard rules — no framework deps in the SDK; brain.js not needed this phase
  </read_first>
  <files>package.json, vitest.config.js, .gitignore</files>
  <action>
    Run `npm init -y`, then set `"type": "module"` in package.json so plain ESM `import x from './x.json'` resolves without Node import-assertion syntax (Vitest and esbuild both resolve JSON imports natively — do NOT use `assert { type: 'json' }` or `with { type: 'json' }`, which vary by Node version and break the browser bundle). Add scripts: `"test": "vitest run"` and `"build": "esbuild src/index.js --bundle --format=iife --global-name=Heed --outfile=dist/sdk.js"`. Install exact pinned dev deps: `npm install -D vitest@4.1.10 happy-dom@20.10.6 esbuild@0.28.1 @playwright/test@1.61.1`. Create vitest.config.js exporting a defineConfig with `test.environment` set to `'happy-dom'`. Create .gitignore containing `node_modules/` and `dist/`. Do NOT add any runtime dependencies — src modules ship zero deps this phase.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.type!=='module') throw new Error('type must be module'); if(!p.scripts.test||!p.scripts.build) throw new Error('missing scripts'); if(!p.devDependencies.vitest||!p.devDependencies['happy-dom']||!p.devDependencies.esbuild) throw new Error('missing dev deps'); if(p.dependencies&&Object.keys(p.dependencies).length) throw new Error('no runtime deps allowed this phase'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e` guard above prints `ok` (exit 0): package.json has type=module, both scripts, all three dev deps, and zero runtime dependencies
    - `npx vitest --version` prints a 4.1.x version (dev deps installed)
    - vitest.config.js sets environment to happy-dom (string match `happy-dom` present)
  </acceptance_criteria>
  <done>Repo has package.json (ESM, scripts, pinned dev deps, no runtime deps), vitest.config.js using happy-dom, and a .gitignore excluding node_modules/ and dist/.</done>
</task>

<task type="auto">
  <name>Task 2: Author failing config tests (CFG-01, CFG-02)</name>
  <read_first>
    - 01-RESEARCH.md "Pattern 1" (lines 165-225) — validateConfig(config, schema) signature and schema shape
    - 01-PATTERNS.md "tests/config.test.js" (lines 215-220) — exact assertion targets
    - CONTRACT.md — the 7 locked data-heed selector values the demo config must carry
  </read_first>
  <files>tests/config.test.js</files>
  <action>
    Write tests/config.test.js importing `validateConfig` from `../src/config.js` (which does not exist yet — this test is RED by design) and importing schema from `../config/schema.json` and the demo config from `../config/demo-platform.json`. Add a `describe('CFG-01', ...)` block asserting: (a) `validateConfig(demoConfig, schema)` returns the config without throwing; (b) the demo config's `selectors` object resolves all seven CONTRACT.md values verbatim — `[data-heed="amount-input"]`, `[data-heed="fee-row"]`, `[data-heed="min-received-row"]`, `[data-heed="proceed-cta"]`, `[data-heed="confirm-cta"]`, `[data-heed="back-btn"]`, and `completionSelector` equal to `[data-heed="flow-complete"]`. Add a `describe('CFG-02', ...)` block asserting `expect(() => validateConfig({ platformId: 'x' }, schema)).toThrow()` (missing required selectors hard-fails) and that a wrong-type value (e.g. selectors as a string) also throws. Use Vitest's `describe/it/expect`.
  </action>
  <verify>
    <automated>npx vitest run tests/config.test.js 2>&1 | grep -Eiq "cannot find module|failed|error" && echo "RED-as-expected" || echo "unexpected-state"</automated>
  </verify>
  <acceptance_criteria>
    - tests/config.test.js exists and references `../src/config.js`, `../config/schema.json`, `../config/demo-platform.json`
    - Running the suite fails (RED) because src/config.js / config files do not exist yet — prints `RED-as-expected`
    - The file contains named blocks whose titles include the literal strings `CFG-01` and `CFG-02`
  </acceptance_criteria>
  <done>A RED config test suite encodes CFG-01 (clean validation + all 7 selectors resolve) and CFG-02 (hard-fail throw on missing/invalid fields), keyed by requirement ID.</done>
</task>

<task type="auto">
  <name>Task 3: Author failing bus + harness tests with decoupled fixtures (BUS-01, TEST-01)</name>
  <read_first>
    - 01-RESEARCH.md "Proving zero direct import" (lines 319-351) — the emitter/subscriber fixture structure verbatim
    - 01-PATTERNS.md "tests/bus.test.js" (lines 177-211) — deep-equal detail assertion (Pitfall 3)
    - 01-VALIDATION.md per-task map row for TEST-01 (line 45) — structural harness assertion
  </read_first>
  <files>tests/bus.test.js, tests/harness.test.js, tests/fixtures/test-emitter.js, tests/fixtures/test-subscriber.js</files>
  <action>
    Create tests/fixtures/test-emitter.js importing only `publish` from `../../src/bus.js`, exporting `emitSynthetic()` that publishes event type `signal:detected` with payload `{ type: 'touch_hesitation', targetSelector: 'confirm-cta', bbox: {}, timestamp: Date.now() }`. Create tests/fixtures/test-subscriber.js importing only `subscribe` from `../../src/bus.js`, exporting `collectReceived(onReceive)` that subscribes to `signal:detected`. Neither fixture may import the other — this is what makes BUS-01's decoupling mechanically checkable. Write tests/bus.test.js in a `describe('BUS-01', ...)` block that wires the two fixtures from the outside: register a `vi.fn()` via collectReceived, call emitSynthetic, then assert the handler was called once AND that its first argument deep-equals the full published payload (not just that it fired — guards Pitfall 3's silent-empty-detail bug). Write tests/harness.test.js in a `describe('TEST-01', ...)` block that reads `test-harness/index.html` from disk with node:fs, parses it via happy-dom's Window/DOMParser, and asserts `querySelectorAll('[data-heed]').length === 7`. All three files reference not-yet-existing modules/files, so the suite is RED by design.
  </action>
  <verify>
    <automated>npx vitest run tests/bus.test.js tests/harness.test.js 2>&1 | grep -Eiq "cannot find module|enoent|failed|error" && echo "RED-as-expected" || echo "unexpected-state"</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/test-emitter.js imports only `../../src/bus.js` (single import) and exports `emitSynthetic`
    - tests/fixtures/test-subscriber.js imports only `../../src/bus.js` (single import) and exports `collectReceived`
    - Neither fixture imports the other (grep of each file shows no reference to the sibling fixture)
    - bus.test.js asserts handler called once AND payload deep-equal; harness.test.js asserts exactly 7 `[data-heed]` elements
    - Running both suites prints `RED-as-expected` (modules/HTML not built yet)
  </acceptance_criteria>
  <done>RED bus and harness suites exist; the two fixtures are provably decoupled (single bus.js import each, no cross-import), encoding BUS-01 and the TEST-01 structural gate by requirement ID.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dev machine | Dev dependencies (vitest, happy-dom, esbuild, @playwright/test) are downloaded and their install scripts run locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm install of vitest/happy-dom/esbuild/@playwright/test | high | mitigate | Install ONLY the four exact pinned versions from 01-RESEARCH.md's Package Legitimacy Audit (lines 83-94). All four are `[VERIFIED: npm registry]` per that audit — flagged SUS only by the "too-new latest-publish" heuristic, each with tens-to-hundreds of millions of weekly downloads and a public, well-known source repo. No `[ASSUMED]`/`[SLOP]` packages present, so no blocking-human checkpoint is required per protocol; esbuild's `postinstall` is its documented platform-binary downloader, not an arbitrary endpoint fetch. Add no packages beyond these four. |
</threat_model>

## Artifacts this phase produces

Symbols and files created by this plan (Wave 0 scaffold):

- `package.json` — ESM (`type: module`), scripts `test` (vitest run) and `build` (esbuild → dist/sdk.js), dev deps vitest@4.1.10 / happy-dom@20.10.6 / esbuild@0.28.1 / @playwright/test@1.61.1; zero runtime dependencies
- `vitest.config.js` — `test.environment: 'happy-dom'`
- `.gitignore` — excludes `node_modules/`, `dist/`
- `tests/config.test.js` — RED suite; blocks `CFG-01`, `CFG-02`
- `tests/bus.test.js` — RED suite; block `BUS-01`
- `tests/harness.test.js` — RED suite; block `TEST-01` (structural: 7 `[data-heed]` elements)
- `tests/fixtures/test-emitter.js` — exports `emitSynthetic()` (publishes `signal:detected`)
- `tests/fixtures/test-subscriber.js` — exports `collectReceived(onReceive)` (subscribes `signal:detected`)

<verification>
- `npx vitest run` executes without a config/runner crash and reports failing (RED) suites for config, bus, and harness — RED is the expected and correct Wave-0 state.
- The two bus fixtures are statically decoupled: each imports only `src/bus.js`; neither imports the other.
</verification>

<success_criteria>
- Dev tooling installed at pinned versions; `npx vitest run` runs the harness end-to-end and reports RED for all three suites (no implementations exist yet).
- Executable assertions exist for CFG-01, CFG-02, BUS-01, and the TEST-01 structural check, each keyed by requirement ID.
</success_criteria>

<output>
Create `.planning/phases/01-config-layer-bus-standalone-test-harness/01-01-SUMMARY.md` when done
</output>
