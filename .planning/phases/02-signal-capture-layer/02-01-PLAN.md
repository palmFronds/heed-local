---
phase: 02-signal-capture-layer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - config/schema.json
  - config/demo-platform.json
  - tests/signal.test.js
  - tests/signal-spa.test.js
autonomous: true
requirements: [SIG-01, SIG-02, SIG-03, SIG-04, SIG-05, SIG-06]

must_haves:
  truths:
    - "config/schema.json accepts an optional top-level signals object (touchHesitation.thresholdMs, scrollReversal.depthThresholdPct, scrollReversal.minReversalDeltaPx) without adding signals to any required array — demo-platform.json still validates cleanly (backward compatible per D-05)"
    - "tests/signal.test.js exists with failing (RED) describe blocks named SIG-01, SIG-02, SIG-03, SIG-04, SIG-05 that import src/signal.js — they fail because src/signal.js does not exist yet, not because of a syntax error"
    - "tests/signal-spa.test.js exists with a failing (RED) describe block named SIG-06, kept in its own file so its MutationObserver assertions never share a file with vi.useFakeTimers() (happy-dom#2097)"
  artifacts:
    - config/schema.json
    - config/demo-platform.json
    - tests/signal.test.js
    - tests/signal-spa.test.js
  key_links:
    - "signal.test.js / signal-spa.test.js import { publish, subscribe } (or the existing collectReceived fixture) from the Phase-1 bus so RED tests assert on real bus receipts, not mocks"
    - "schema.json's optional signals fields are the config surface src/signal.js reads via config.signals?.x?.y ?? default in Plan 02/03"
---

<objective>
Author the Wave-0 RED test suites for all six signal requirements and extend the config
surface with the optional signal-threshold fields the implementation plans will read.

This plan writes NO production signal-capture logic. It establishes the failing tests that
Plans 02 and 03 flip green, and lands the additive, backward-compatible config schema change
that D-05 ("scroll-reversal delta must be configurable") requires.

Purpose: Nyquist RED-first — every SIG requirement has a failing automated check on disk before
any implementation is written, so later plans are measured against a fixed target.
Output: config/schema.json (+ demo-platform.json) extended; tests/signal.test.js and
tests/signal-spa.test.js RED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signal-capture-layer/02-CONTEXT.md
@.planning/phases/02-signal-capture-layer/02-RESEARCH.md
@.planning/phases/02-signal-capture-layer/02-PATTERNS.md
@config/schema.json
@config/demo-platform.json
@tests/bus.test.js
@tests/fixtures/test-subscriber.js
</context>

## Artifacts this phase produces

This plan (02-01) creates/extends:
- `config/schema.json` — adds optional `signals` object (fields: `touchHesitation.thresholdMs`, `scrollReversal.depthThresholdPct`, `scrollReversal.minReversalDeltaPx`)
- `config/demo-platform.json` — adds a concrete `signals` block with the default values
- `tests/signal.test.js` — RED describe blocks: `SIG-01`, `SIG-02`, `SIG-03`, `SIG-04`, `SIG-05`
- `tests/signal-spa.test.js` — RED describe block: `SIG-06`

Later plans in this phase add: `src/signal.js` (functions `attachListeners`, `initSignalCapture`, `buildPayload`, `maybeReattach`, per-signal `wire*` helpers), `src/index.js` wiring, `test-harness/index.html` rewiring, `playwright.config.js`, `tests/e2e/harness.spec.js`.

<tasks>

<task type="auto">
  <name>Task 1: Extend config schema and demo config with optional signal-threshold fields (D-05)</name>
  <files>config/schema.json, config/demo-platform.json</files>
  <read_first>
    - config/schema.json (the file being modified — full current content)
    - config/demo-platform.json (the file being modified)
    - .planning/phases/02-signal-capture-layer/02-PATTERNS.md (section "config/schema.json (modified)" — exact extension shape)
    - .planning/phases/02-signal-capture-layer/02-RESEARCH.md (Open Question 1 + Recommended Project Structure "Note on config/schema.json")
    - src/config.js (to confirm walk() only enforces keys in a node's own required array)
  </read_first>
  <action>
    In config/schema.json, add a new optional top-level property "signals" of type object, sibling
    to "selectors". Do NOT add "signals" to the top-level required array (this preserves backward
    compatibility — src/config.js's walk() only enforces keys present in a node's own required
    array, verified in RESEARCH.md). The signals object declares two nested optional objects:
    "touchHesitation" with property "thresholdMs" (type number), and "scrollReversal" with
    properties "depthThresholdPct" (type number) and "minReversalDeltaPx" (type number). No nested
    required arrays — every new field is optional. In config/demo-platform.json, add a concrete
    "signals" block supplying thresholdMs 800, depthThresholdPct 0.4, minReversalDeltaPx 50 (these
    are the defaults RESEARCH.md derives; making them explicit in demo config proves the schema
    accepts the shape and gives Plan 02/03 a real config surface to read). Preserve the existing
    "$schema", "platformId", "completionSelector", and all 7 "selectors" entries exactly — this
    task only adds, never removes or renames (the 7 locked data-heed selectors are untouchable per
    CONTRACT.md).
  </action>
  <verify>
    <automated>npx vitest run tests/config.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/config.test.js` still passes — the existing config validator accepts demo-platform.json with the new signals block (no required-field regression).
    - config/schema.json's top-level `required` array is unchanged (still exactly `["platformId", "selectors", "completionSelector"]`) — `signals` is NOT added to it.
    - config/demo-platform.json contains `signals.touchHesitation.thresholdMs === 800`, `signals.scrollReversal.depthThresholdPct === 0.4`, `signals.scrollReversal.minReversalDeltaPx === 50`.
    - All 7 `data-heed` selector strings in demo-platform.json are byte-for-byte unchanged.
  </acceptance_criteria>
  <done>schema.json declares optional signals fields, demo-platform.json supplies concrete defaults, and the Phase-1 config test suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Author RED unit suites for SIG-01, SIG-02, SIG-03, SIG-04, SIG-05 in tests/signal.test.js</name>
  <files>tests/signal.test.js</files>
  <read_first>
    - tests/bus.test.js (analog — describe/it/expect + subscribe-then-act-then-assert structure)
    - tests/fixtures/test-subscriber.js (collectReceived helper to reuse)
    - .planning/phases/02-signal-capture-layer/02-RESEARCH.md (Code Examples for touch hesitation, blur, scroll; PII-Safety Verification section; Phase Requirements → Test Map)
    - .planning/phases/02-signal-capture-layer/02-PATTERNS.md (section "tests/signal.test.js" — conventions to copy)
    - .planning/phases/02-signal-capture-layer/02-VALIDATION.md (Per-Task Verification Map — commands per SIG)
  </read_first>
  <action>
    Create tests/signal.test.js importing the public entry point from ../src/signal.js (expected
    export attachListeners, and initSignalCapture) — this import is what makes the whole suite RED
    until Plan 02/03 create the file. Use happy-dom's ambient global document (vitest.config.js
    already sets environment happy-dom) to build detached fixture DOM via document.body.innerHTML
    with the 7 data-heed elements from demo-platform.json. Write one describe block per requirement,
    named exactly "SIG-01", "SIG-02", "SIG-03", "SIG-04", "SIG-05" (the -t filter matches the
    describe name). SIG-01: use vi.useFakeTimers(); dispatch touchstart on the proceedCta element,
    advance timers past 800ms, assert exactly one touch_hesitation bus receipt; a second case
    dispatches touchstart then touchend before 800ms and asserts NO receipt (this is the emergent
    tap behavior — do NOT write a separate <300ms threshold assertion, per Pitfall 1). SIG-02:
    focus then blur amountInput while its value is empty asserts one blur_incomplete receipt; a
    second case sets amountInput.value to a non-empty string then blurs and asserts NO receipt
    (D-03 final-value-diff). SIG-03: stub window.scrollY and window.innerHeight explicitly via
    Object.defineProperty (happy-dom has no layout engine — Pitfall 5), dispatch scroll events
    crossing 40% depth then reversing by at least the configured min delta, assert one
    scroll_reversal receipt; a second case reverses by less than the min delta and asserts NO
    receipt (D-05 hysteresis). SIG-04: dispatch a popstate while the cached flowComplete flag is
    false and assert a back_intent receipt (this case may need initSignalCapture wiring — keep it
    RED and minimal). SIG-05: for each of the four signal types, capture the emitted payload and
    assert Object.keys(payload).sort() deep-equals the exact allow-list for that type
    (touch_hesitation and blur_incomplete: type, targetSelector, bbox, timestamp; scroll_reversal:
    type, targetSelector, bbox, scrollDepth, timestamp; back_intent: type, targetSelector, bbox,
    pathname, timestamp per D-07), and specifically assert no key named value/text/id/class/amount
    is ever present even when the fixture element carries a non-empty value. Keep vi.useFakeTimers()
    usage confined to this file; do NOT put any MutationObserver-dependent assertion here (that goes
    in signal-spa.test.js — happy-dom#2097). Every test must currently FAIL (module not found /
    functions undefined), and must fail cleanly (no syntax/parse error).
  </action>
  <verify>
    <automated>npx vitest run tests/signal.test.js 2>&1 | grep -Eiq "cannot find|failed|no test found|error" && echo "RED confirmed" || (echo "expected RED but suite did not fail" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - tests/signal.test.js contains describe blocks named exactly `SIG-01`, `SIG-02`, `SIG-03`, `SIG-04`, `SIG-05`.
    - The suite is RED because `../src/signal.js` cannot be resolved (or its exports are undefined) — verified by the command finding a failure, NOT a parse/syntax error in the test file itself.
    - SIG-01 includes both a hold-past-800ms case (expects a receipt) and a release-before-800ms case (expects none) with NO separate `<300ms` threshold assertion.
    - SIG-05 asserts `Object.keys(payload).sort()` deep-equality against an explicit allow-list for all four signal types, including a case where the blur fixture element has a non-empty value yet no value-bearing key appears in the payload.
    - The file contains no MutationObserver-based assertion (SIG-06 lives in signal-spa.test.js).
  </acceptance_criteria>
  <done>tests/signal.test.js is on disk, RED for the correct reason (missing implementation), and encodes SIG-01/02/03/04/05 including the SIG-05 structural allow-list.</done>
</task>

<task type="auto">
  <name>Task 3: Author RED SPA re-attachment suite for SIG-06 in tests/signal-spa.test.js</name>
  <files>tests/signal-spa.test.js</files>
  <read_first>
    - tests/harness.test.js (happy-dom Window construction pattern)
    - tests/bus.test.js (assertion/describe style)
    - .planning/phases/02-signal-capture-layer/02-RESEARCH.md (Architecture Pattern 1; Common Pitfalls #2, #3, #6; PII note)
    - .planning/phases/02-signal-capture-layer/02-PATTERNS.md (section "tests/signal-spa.test.js" — direct-function-call strategy)
  </read_first>
  <action>
    Create tests/signal-spa.test.js importing attachListeners and maybeReattach (and
    initSignalCapture) from ../src/signal.js — again RED until Plan 03. Write a single describe
    block named exactly "SIG-06". Follow the direct-function-call strategy from Pitfall 3: rather
    than relying on asynchronous MutationObserver callback delivery, mutate document.body.innerHTML
    to simulate a route swap, update window.location.pathname (or the test's pathname source), then
    call maybeReattach(config) directly and assert synchronously. Cover: (a) 3+ consecutive
    simulated navigations each attach listeners exactly once per navigation — assert that a spy on
    the bus (or a counter) shows no duplicate signal firing when the same event is dispatched after
    re-attachment; (b) after a route swap the NEW element (a different object identity) receives a
    fresh listener while a re-run of maybeReattach on an unchanged pathname does NOT re-attach
    (WeakSet idempotency keyed on element identity, not selector string — the silent
    under-attachment failure mode Success Criterion 6 calls out); (c) dispatching the same
    touch/blur sequence twice on one element after multiple maybeReattach calls fires the signal
    only once (no double-listener). Do NOT use vi.useFakeTimers() anywhere in this file (keep it
    separate from signal.test.js per happy-dom#2097). Every test must currently FAIL because
    src/signal.js does not exist yet, and must fail cleanly.
  </action>
  <verify>
    <automated>npx vitest run tests/signal-spa.test.js 2>&1 | grep -Eiq "cannot find|failed|no test found|error" && echo "RED confirmed" || (echo "expected RED but suite did not fail" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - tests/signal-spa.test.js contains a describe block named exactly `SIG-06`.
    - The suite is RED because `../src/signal.js` is unresolved — not a parse/syntax error.
    - Tests invoke `maybeReattach(config)` directly (no reliance on async MutationObserver delivery) and assert synchronously.
    - The file contains NO `vi.useFakeTimers()` call.
    - At least one case asserts idempotency: re-running re-attachment on an unchanged pathname does not add a second listener (no double-firing).
  </acceptance_criteria>
  <done>tests/signal-spa.test.js is on disk, RED for the missing-implementation reason, isolated from fake-timer usage, and encodes SIG-06 idempotency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host-page DOM → signal.js listeners | Raw DOM events (touch/blur/scroll/popstate) cross into the SDK; only geometry/timing may be read, never field values |
| config file → validateConfig | Untrusted config shape crosses into the SDK at init; new optional signals fields must not weaken the existing hard-fail validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | signal payloads (SIG-05) | high | mitigate | RED structural allow-list test authored here (SIG-05 describe block) locks the payload key set before any implementation exists — any future PII-shaped field fails the suite immediately |
| T-02-02 | Tampering | config/schema.json optional signals fields | low | accept | New fields are optional and non-required; adding them cannot bypass the Phase-1 hard-fail validator (walk() still enforces every existing required key). Verified by re-running tests/config.test.js |
| T-02-SC | Tampering | npm installs | low | accept | This phase installs NO new packages (RESEARCH.md Package Legitimacy Audit: N/A). No install task exists; nothing to gate |
</threat_model>

<verification>
- `npx vitest run tests/config.test.js` green (config regression check).
- `npx vitest run tests/signal.test.js tests/signal-spa.test.js` RED for the correct reason (missing src/signal.js), no parse errors.
- schema.json top-level `required` unchanged; demo-platform.json's 7 selectors byte-identical.
</verification>

<success_criteria>
- Optional `signals` config surface exists and is backward compatible (D-05).
- All six SIG requirements have a failing automated check on disk (Nyquist RED-first).
- SIG-05 structural allow-list assertion present for all four signal types.
</success_criteria>

<output>
Create `.planning/phases/02-signal-capture-layer/02-01-SUMMARY.md` when done.
</output>
