---
phase: 02-signal-capture-layer
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/signal.js
  - src/index.js
autonomous: true
requirements: [SIG-04, SIG-06]

must_haves:
  truths:
    - "A popstate event while the cached flowCompleteFlag is false emits exactly one back_intent carrying { pathname } (D-07); a popstate after the completion selector has appeared (flag true) emits nothing (SIG-04, D-06)"
    - "flowCompleteFlag is set once by the shared MutationObserver callback when the completion selector first resolves — never by a live document.querySelector inside the popstate handler (D-06)"
    - "3+ consecutive SPA navigations re-attach listeners exactly once per navigation via a single WeakSet<Element> and one pathname-gated maybeReattach() reached from BOTH the MutationObserver and the popstate listener — no duplicate firing, no silent under-attachment (SIG-06)"
    - "initSignalCapture(config) is wired into src/index.js init() after validateConfig succeeds, without changing init()'s returned shape { config, publish, subscribe }"
  artifacts:
    - src/signal.js
    - src/index.js
  key_links:
    - "one WeakSet<Element> attachedElements gates idempotency keyed on element identity, not selector string (the anti-silent-under-attachment invariant)"
    - "one MutationObserver on document.body serves two jobs: pathname-diff re-attachment AND setting the cached flowCompleteFlag (D-06) — never two observers"
    - "src/index.js imports initSignalCapture from './signal.js' and calls it inside init() post-validation; initDemo() inherits it transitively"
---

<objective>
Complete src/signal.js by adding the SPA-safety layer — a single WeakSet-idempotent, pathname-gated
re-attachment path reached from both a MutationObserver and the popstate listener (SIG-06) — and the
back-intent handler gated on a cached flowComplete flag (SIG-04, D-06). Then wire
initSignalCapture(config) into src/index.js's init() so the harness and any partner page instrument
signals at startup.

Purpose: flip the remaining SIG-04 and SIG-06 RED tests green and make signal capture actually run
under init() — closing the phase's core deliverable.
Output: src/signal.js SPA/back-intent layer + initSignalCapture entry; src/index.js wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signal-capture-layer/02-CONTEXT.md
@.planning/phases/02-signal-capture-layer/02-RESEARCH.md
@.planning/phases/02-signal-capture-layer/02-PATTERNS.md
@src/index.js
@src/signal.js
@config/demo-platform.json
@tests/signal-spa.test.js
@tests/signal.test.js
</context>

## Artifacts this plan produces

- `src/signal.js` (MODIFIED) — adds:
  - module-scoped `attachedElements` (WeakSet), `lastPathname`, `flowCompleteFlag`
  - `checkFlowComplete(config)` — sets the cached flag once (D-06)
  - `maybeReattach(config)` — single pathname-gated re-attach path (SIG-06)
  - back-intent handling inside the popstate listener (SIG-04, D-07)
  - `initSignalCapture(config)` — initial attach + MutationObserver + popstate wiring
- `src/index.js` (MODIFIED) — imports and calls `initSignalCapture(config)` inside `init()`
- No changes to the returned public API shape or to the 7 locked selectors.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WeakSet-idempotent, pathname-gated re-attachment core (SIG-06)</name>
  <files>src/signal.js</files>
  <read_first>
    - src/signal.js (the file being modified — attachListeners/resolveTargets from Plan 02-02)
    - .planning/phases/02-signal-capture-layer/02-RESEARCH.md (Pattern 1: Single WeakSet, Single Pathname-Gated Reattach; Common Pitfalls #2, #3)
    - .planning/phases/02-signal-capture-layer/02-PATTERNS.md (Pattern 1 rationale — WeakSet keyed on element, not selector string)
    - tests/signal-spa.test.js (SIG-06 describe block)
  </read_first>
  <behavior>
    - attachListeners skips any element already in the WeakSet (idempotent); a fresh element (new object identity) after a route swap receives a real listener.
    - maybeReattach compares window.location.pathname against lastPathname; only on a difference does it update lastPathname and call attachListeners; on an unchanged pathname it re-checks flowComplete but does NOT re-attach.
    - Running maybeReattach repeatedly on an unchanged pathname never double-attaches (no duplicate signal firing).
  </behavior>
  <action>
    Introduce a module-scoped attachedElements = new WeakSet(), lastPathname initialized to
    window.location.pathname, and flowCompleteFlag = false. Refactor attachListeners(config) so each
    resolved target is skipped when attachedElements.has(el) and otherwise added to the WeakSet
    before its handler wiring runs — this makes the touch/blur wiring from Plan 02-02 idempotent per
    element. Add checkFlowComplete(config): if flowCompleteFlag is already true, return; else
    querySelector config.selectors.flowComplete (or config.completionSelector) and set the flag true
    when it resolves non-null (once true, stays true for the session — D-06). Call checkFlowComplete
    at the end of every attachListeners pass. Add maybeReattach(config): read current pathname; if
    it differs from lastPathname, set lastPathname to it and call attachListeners(config); else call
    checkFlowComplete(config) (the completion element may have just appeared on the same route).
    Add a comment explaining the WeakSet is keyed on element identity (not selector string) so a
    route swap's new element correctly re-attaches while GC frees the old entry — the anti-silent-
    under-attachment invariant of SIG-06. Export maybeReattach and attachListeners as named exports
    for the SIG-06 direct-call tests.
  </action>
  <verify>
    <automated>npx vitest run tests/signal-spa.test.js -t "SIG-06"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/signal-spa.test.js -t "SIG-06"` passes: 3+ navigations re-attach once each; unchanged-pathname re-runs do not double-attach; new post-swap elements get fresh listeners.
    - Exactly one `WeakSet` is used for attachment idempotency, keyed on the element object (not a selector string).
    - `maybeReattach` is the only function that diffs `location.pathname` and the only path that calls `attachListeners` for re-attachment.
    - `checkFlowComplete` sets `flowCompleteFlag` at most once and never clears it.
  </acceptance_criteria>
  <done>SPA re-attachment is WeakSet-idempotent and pathname-gated through a single maybeReattach path; SIG-06 test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add back-intent handler and initSignalCapture wiring one MutationObserver + popstate (SIG-04, D-06, D-07)</name>
  <files>src/signal.js</files>
  <read_first>
    - src/signal.js (the file being modified — maybeReattach/checkFlowComplete from Task 1)
    - .planning/phases/02-signal-capture-layer/02-RESEARCH.md (Pattern 1 initSignalCapture example; Back Intent with Cached flowComplete section; Anti-Patterns — one observer two jobs)
    - tests/signal.test.js (SIG-04 describe block)
  </read_first>
  <behavior>
    - popstate while flowCompleteFlag is false publishes one back_intent with { pathname: window.location.pathname }.
    - popstate while flowCompleteFlag is true publishes nothing.
    - The popstate handler reads the cached flag only — it never runs a live querySelector for completion.
    - A single MutationObserver on document.body drives both re-attachment (via maybeReattach) and the flowComplete flag update.
  </behavior>
  <action>
    Add initSignalCapture(config) that: (1) calls attachListeners(config) for the initial attach;
    (2) creates ONE MutationObserver whose callback calls maybeReattach(config) — reference the
    Anti-Pattern comment that this same observer, via maybeReattach → checkFlowComplete, also updates
    flowCompleteFlag, so no second observer is created (D-06); observe document.body with { childList:
    true, subtree: true }; (3) registers a single window popstate listener that first calls
    maybeReattach(config) then, if flowCompleteFlag is false, publishes back_intent via
    publish('signal:detected', buildPayload('back_intent', { pathname: window.location.pathname }))
    (D-07 pathname field, null targetSelector/bbox). Add a comment on the popstate handler noting it
    reads the cached flag, never a live DOM query (D-06). Guard initSignalCapture so repeated calls
    do not stack multiple observers/popstate listeners (a module-scoped initialized boolean is
    acceptable — window and document.body are singletons). Export initSignalCapture.
  </action>
  <verify>
    <automated>npx vitest run tests/signal.test.js -t "SIG-04"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/signal.test.js -t "SIG-04"` passes: popstate with flag false emits one back_intent; with flag true emits none.
    - The back_intent payload carries `pathname` with null `targetSelector`/`bbox` (D-07) — verified via the SIG-05 allow-list already green.
    - Exactly one `MutationObserver` is instantiated in signal.js (source review) and it drives both re-attachment and the flowComplete flag.
    - The popstate handler references `flowCompleteFlag` and contains no `querySelector` call for the completion selector (D-06).
  </acceptance_criteria>
  <done>Back intent fires on popstate gated by the cached flowComplete flag (D-06), one observer serves two jobs; SIG-04 test green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire initSignalCapture(config) into src/index.js init() (post-validation)</name>
  <files>src/index.js</files>
  <read_first>
    - src/index.js (the file being modified — full current content)
    - .planning/phases/02-signal-capture-layer/02-PATTERNS.md (section "src/index.js (modified)" — wiring point and return-shape discipline)
    - src/signal.js (initSignalCapture export from Task 2)
    - tests/index.test.js (existing init() contract test to keep green)
  </read_first>
  <action>
    In src/index.js add import { initSignalCapture } from './signal.js'. Inside init(rawConfig),
    after const config = validateConfig(rawConfig, schema) succeeds and before the return, call
    initSignalCapture(config). Add a comment in the existing "why" style explaining signal listeners
    attach only after hard-fail validation passes (mirroring the existing note that an invalid config
    must stop initialization) — never instrument the DOM against an unvalidated config. Do NOT add a
    signal key to init()'s return; keep the returned shape exactly { config, publish, subscribe }
    (return-shape discipline — signal capture is side-effecting, not a returned value). initDemo()
    needs no change; it inherits the wiring transitively through init().
  </action>
  <verify>
    <automated>npx vitest run tests/index.test.js && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/index.test.js` passes — init()'s returned shape is still exactly `{ config, publish, subscribe }` (no new key).
    - `npx vitest run` (full suite) is fully green, including all SIG-01 through SIG-06.
    - init() calls initSignalCapture only after validateConfig succeeds (source review — the call sits below the validateConfig line, above the return).
    - initDemo() is unchanged.
  </acceptance_criteria>
  <done>init() instruments signal capture post-validation without changing its public contract; full vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host SPA router → MutationObserver/popstate | client-side navigation events cross into signal.js; re-attachment must not leak or duplicate, and back_intent must carry only a pathname |
| config → init() → initSignalCapture | signal instrumentation must run only against a hard-fail-validated config |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | back_intent payload (SIG-05/D-07) | high | mitigate | back_intent routes through the same buildPayload allow-list — carries only pathname + timestamp, never element content; enforced by the green SIG-05 structural test |
| T-02-04 | Denial of Service | MutationObserver on document.body | low | accept | Pathname gate in maybeReattach prevents a full selector sweep on every DOM mutation (Pitfall 2) — bounded work per genuine navigation, not per keystroke |
| T-02-05 | Spoofing | synthetic touchstart/popstate from a compromised host page | low | accept | v1 does not check event.isTrusted (RESEARCH.md Security Domain) — out of scope for the first-party harness; flagged as a v2 hardening consideration if embedded on untrusted pages |
| T-02-SC | Tampering | npm installs | low | accept | No new packages this phase — no install task to gate |
</threat_model>

<verification>
- `npx vitest run` full suite green — SIG-01 through SIG-06 all passing.
- Source review: one WeakSet, one MutationObserver, popstate reads cached flag only, init() call post-validation, return shape unchanged.
</verification>

<success_criteria>
- SIG-04 (back intent, D-06/D-07) and SIG-06 (SPA re-attachment) tests green.
- signal capture runs under init()/initDemo() with the public contract unchanged.
- Full vitest suite green.
</success_criteria>

<output>
Create `.planning/phases/02-signal-capture-layer/02-03-SUMMARY.md` when done.
</output>
