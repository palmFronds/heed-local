---
phase: 02-signal-capture-layer
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - src/signal.js
autonomous: true
requirements: [SIG-01, SIG-02, SIG-03, SIG-05]

must_haves:
  truths:
    - "Pressing and holding a CTA element (proceedCta/confirmCta/backBtn only, per D-02) for 800ms+ emits exactly one touch_hesitation via a single live setTimeout (D-01); releasing before 800ms emits nothing and requires no separate <300ms check"
    - "Blurring amountInput (the only monitored input, D-04) while its value is empty emits blur_incomplete; blurring while non-empty emits nothing (D-03 final-value diff), and amountInput.value is never read inside buildPayload"
    - "Scrolling past 40% viewport depth then reversing by at least the configured min delta (default 50px, D-05) emits scroll_reversal; a reversal under the delta emits nothing"
    - "buildPayload is the single choke point constructing every emitted payload; each signal type's key set matches its allow-list exactly (SIG-05), with scroll_reversal/back_intent carrying null targetSelector/bbox plus scrollDepth/pathname respectively (D-07)"
  artifacts:
    - src/signal.js
  key_links:
    - "src/signal.js imports only { publish } from './bus.js' (producer, never subscriber) and publishes on the 'signal:detected' bus type"
    - "signal.js reads thresholds via config.signals?.touchHesitation?.thresholdMs ?? 800 and config.signals?.scrollReversal?.{depthThresholdPct ?? 0.4, minReversalDeltaPx ?? 50} — the optional surface added in Plan 02-01"
    - "buildPayload(type, ctx) reads ctx.el.getBoundingClientRect() for bbox but never ctx.el.value/.textContent — the PII firewall for SIG-05"
---

<objective>
Create src/signal.js and implement the three DOM-element/window signal handlers whose tests were
authored RED in Plan 02-01: touch hesitation (SIG-01), blur incomplete (SIG-02), and scroll
reversal (SIG-03) — all publishing through one centralized, PII-safe buildPayload choke point
(SIG-05). This plan does NOT implement back intent or SPA re-attachment (Plan 02-03) and does NOT
wire signal.js into index.js (Plan 02-03).

Purpose: flip the SIG-01/02/03/05 RED tests green while establishing the buildPayload firewall that
makes SIG-05's PII guarantee structural rather than incidental.
Output: src/signal.js with attachListeners scaffold, buildPayload, and the touch/blur/scroll wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signal-capture-layer/02-CONTEXT.md
@.planning/phases/02-signal-capture-layer/02-RESEARCH.md
@.planning/phases/02-signal-capture-layer/02-PATTERNS.md
@src/bus.js
@config/demo-platform.json
@tests/signal.test.js
</context>

## Artifacts this plan produces

- `src/signal.js` (NEW) — introduces functions:
  - `buildPayload(type, ctx)` — single PII-safe payload choke point (SIG-05, D-07)
  - `resolveTargets(config)` — resolves in-DOM elements for the monitored selectors
  - `attachListeners(config)` — entry point that wires the three handlers (extended in Plan 02-03)
  - `wireTouchHesitation(el, selectorValue, config)` — SIG-01 / D-01 / D-02
  - `wireBlurIncomplete(el, selectorValue)` — SIG-02 / D-03 / D-04
  - `attachScrollReversal(config)` — SIG-03 / D-05
- The `WeakSet` idempotency set, `maybeReattach`, MutationObserver, cached flowComplete flag, back-intent handler, `initSignalCapture`, and index.js wiring are added in Plan 02-03.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/signal.js with the centralized PII-safe buildPayload choke point (SIG-05, D-07)</name>
  <files>src/signal.js</files>
  <read_first>
    - src/bus.js (import { publish }; the "single choke point" comment style to mirror)
    - src/config.js (defensive optional-chaining style; [heed] error-prefix convention)
    - .planning/phases/02-signal-capture-layer/02-RESEARCH.md (Pattern 3: Centralized PII-Safe Payload Builder; PII-Safety Verification section)
    - .planning/phases/02-signal-capture-layer/02-PATTERNS.md (section "src/signal.js" — choke-point comment style, plain-function-export convention)
    - tests/signal.test.js (the SIG-05 describe block this must satisfy)
  </read_first>
  <behavior>
    - buildPayload('touch_hesitation', { el, targetSelector }) returns keys exactly: type, targetSelector, bbox, timestamp — bbox is { x, y, width, height } from getBoundingClientRect().
    - buildPayload('blur_incomplete', { el, targetSelector }) returns the same key set; never reads el.value.
    - buildPayload('scroll_reversal', { scrollDepth }) returns type, targetSelector (null), bbox (null), scrollDepth, timestamp (D-07).
    - buildPayload('back_intent', { pathname }) returns type, targetSelector (null), bbox (null), pathname, timestamp (D-07).
    - buildPayload('unknown') throws Error with a message beginning [heed].
  </behavior>
  <action>
    Create src/signal.js as a plain-function ES module (no classes, no default export, matching
    bus.js/config.js). Import { publish } from './bus.js' only — signal.js is a producer, never a
    subscriber. Implement buildPayload(type, ctx) as the sole function that constructs the object
    later passed to publish. Give it a header comment in the established "why, not what" style
    stating it is the single choke point enforcing SIG-05's PII allow-list and that any field read
    from a DOM element here other than getBoundingClientRect() is a No-PII-rule violation. Use a
    switch on type with an explicit per-type object literal (the allow-list): touch_hesitation and
    blur_incomplete build { type, targetSelector, bbox, timestamp } where bbox destructures x/y/
    width/height off getBoundingClientRect(); scroll_reversal builds { type, targetSelector: null,
    bbox: null, scrollDepth, timestamp }; back_intent builds { type, targetSelector: null,
    bbox: null, pathname, timestamp } (D-07's deliberate deviation from REQUIREMENTS.md's literal
    4-field shape — reference D-07 in the comment). timestamp comes from Date.now() computed once at
    the top of the function. The default branch throws new Error with a message prefixed [heed]
    naming the unknown type. Do NOT read el.value, el.textContent, el.innerHTML, localStorage, or
    document.cookie anywhere in this function. Export buildPayload (and the other functions added in
    later tasks) as named exports.
  </action>
  <verify>
    <automated>npx vitest run tests/signal.test.js -t "SIG-05"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/signal.test.js -t "SIG-05"` passes (all four allow-list assertions green).
    - buildPayload is the only function in src/signal.js that constructs the object passed to publish.
    - A source read of buildPayload shows no reference to `.value`, `.textContent`, `.innerHTML`, `localStorage`, or `document.cookie` inside it (the choke point stays PII-clean; region-scoped to the buildPayload function body — the blur handler in Task 3 legitimately reads value elsewhere).
    - The unknown-type branch throws an Error whose message starts with `[heed]`.
    - src/signal.js imports `{ publish }` from './bus.js' and does not import `subscribe`.
  </acceptance_criteria>
  <done>src/signal.js exists; buildPayload enforces the SIG-05 allow-list for all four types including D-07's null/extra-field shape; SIG-05 test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement live-firing touch-hesitation timer, CTA-scoped (SIG-01, D-01, D-02)</name>
  <files>src/signal.js</files>
  <read_first>
    - src/signal.js (the file being modified — buildPayload from Task 1)
    - config/demo-platform.json (selector values for proceedCta/confirmCta/backBtn)
    - .planning/phases/02-signal-capture-layer/02-RESEARCH.md (Pattern 2: Live-Firing Hold Timer; Common Pitfalls #1)
    - tests/signal.test.js (SIG-01 describe block)
  </read_first>
  <behavior>
    - touchstart on a CTA element schedules one setTimeout at the configured threshold (default 800ms).
    - If touchend/touchcancel/touchmove fires before the timer elapses, clearTimeout cancels it — no signal.
    - If the timer elapses, one touch_hesitation payload publishes immediately (live, while held) and the timer id is nulled so a later touchend is a no-op (single emission).
    - Only proceedCta, confirmCta, backBtn are wired — amountInput/feeRow/minReceivedRow/flowComplete are NOT touch-monitored (D-02).
  </behavior>
  <action>
    Add resolveTargets(config) that returns { el, selectorKey, selectorValue } entries for the
    monitored selectors that are actually present in the DOM (via document.querySelector against
    config.selectors[key]). Add wireTouchHesitation(el, selectorValue, config) implementing the
    single-timer pattern: read HESITATION_THRESHOLD_MS as config.signals?.touchHesitation?.thresholdMs
    ?? 800; on touchstart start a setTimeout that, on elapse, nulls its own timer id then calls
    publish('signal:detected', buildPayload('touch_hesitation', { el, targetSelector: selectorValue }));
    a shared cancel() clears the timer id if non-null and nulls it, wired to touchend, touchcancel,
    and touchmove. Register all touch listeners with { passive: true } (never call preventDefault in
    a passive listener — Anti-Patterns). Add a comment referencing D-01 (live fire, not on touchend)
    and Pitfall 1 (no second <300ms check — the single timer produces tap-rejection for free). In
    attachListeners(config), iterate resolveTargets and call wireTouchHesitation ONLY for the CTA
    selector keys proceedCta, confirmCta, backBtn (D-02 scope — reference D-02 in a comment
    explaining feeRow/minReceivedRow are scroll-past, not hold, targets).
  </action>
  <verify>
    <automated>npx vitest run tests/signal.test.js -t "SIG-01"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/signal.test.js -t "SIG-01"` passes: hold-past-threshold emits exactly one touch_hesitation; release-before-threshold emits none.
    - Touch hesitation is wired only for proceedCta/confirmCta/backBtn — a source review confirms amountInput/feeRow/minReceivedRow are not passed to wireTouchHesitation.
    - The implementation uses exactly one setTimeout per touchstart and no second timer or stored-timestamp `<300ms` comparison (Pitfall 1).
    - All touch listeners use `{ passive: true }` and no `preventDefault()` call exists in signal.js.
  </acceptance_criteria>
  <done>Touch hesitation fires live at the configured threshold, CTA-scoped per D-02, single-emission per D-01; SIG-01 test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement blur-incomplete (SIG-02, D-03, D-04) and scroll-reversal (SIG-03, D-05)</name>
  <files>src/signal.js</files>
  <read_first>
    - src/signal.js (the file being modified — buildPayload + attachListeners from Tasks 1-2)
    - .planning/phases/02-signal-capture-layer/02-RESEARCH.md (Code Examples: Scroll Reversal with Hysteresis Delta; Pattern 3 blur note; Common Pitfalls #4, #5, #6)
    - tests/signal.test.js (SIG-02 and SIG-03 describe blocks)
  </read_first>
  <behavior>
    - Blur on amountInput while its value is empty publishes one blur_incomplete; blur while value is non-empty publishes nothing (D-03).
    - The empty-check reads el.value only to decide whether to publish; that value never reaches buildPayload (SIG-05).
    - Scroll past depthThresholdPct (default 0.4) sets a crossed flag; a subsequent reversal of at least minReversalDeltaPx (default 50) publishes one scroll_reversal carrying scrollDepth; a smaller reversal publishes nothing.
  </behavior>
  <action>
    Add wireBlurIncomplete(el, selectorValue): on blur, read el.value into a local boolean isEmpty
    (el.value === '') used ONLY to gate the publish call; if empty, publish('signal:detected',
    buildPayload('blur_incomplete', { el, targetSelector: selectorValue })). The value string is
    never passed into ctx or buildPayload — add a comment marking this as the SIG-05 firewall (value
    read here, never in buildPayload). In attachListeners, wire wireBlurIncomplete ONLY for the
    amountInput selector (D-04 — the only input in the flow). Add attachScrollReversal(config)
    reading DEPTH_THRESHOLD_PCT (config.signals?.scrollReversal?.depthThresholdPct ?? 0.4) and
    MIN_REVERSAL_DELTA_PX (config.signals?.scrollReversal?.minReversalDeltaPx ?? 50). Maintain
    module-scoped maxScrollY, thresholdCrossed, and a rafScheduled boolean. The window scroll
    listener (passive) coalesces per frame via requestAnimationFrame using rafScheduled as a
    re-entrancy gate (NOT as a time throttle — Pitfall 6; add a comment stating rAF coalesces per
    frame, it does not slow the cadence). Inside the rAF callback compute depthPct = scrollY /
    innerHeight; if scrollY exceeds maxScrollY, update maxScrollY, set thresholdCrossed when depthPct
    >= DEPTH_THRESHOLD_PCT, and return; otherwise if thresholdCrossed and (maxScrollY - scrollY) >=
    MIN_REVERSAL_DELTA_PX, publish scroll_reversal with { scrollDepth: depthPct } and reset
    thresholdCrossed to false (one emission per down-then-up cycle, re-arms on next descent). Call
    attachScrollReversal from attachListeners (window-level, attach once — guard against re-adding
    the window scroll listener on repeat attachListeners calls; a module-scoped
    scrollListenerAttached boolean is acceptable here since window is a singleton).
  </action>
  <verify>
    <automated>npx vitest run tests/signal.test.js -t "SIG-02" && npx vitest run tests/signal.test.js -t "SIG-03"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/signal.test.js -t "SIG-02"` passes: empty-value blur emits blur_incomplete; non-empty-value blur emits nothing.
    - `npx vitest run tests/signal.test.js -t "SIG-03"` passes: cross-40%-then-reverse-by-≥min-delta emits scroll_reversal; sub-delta reversal emits nothing.
    - The blur handler reads `el.value` only for the emptiness gate; `buildPayload`'s blur_incomplete branch still contains no value read (SIG-05 firewall intact).
    - Blur monitoring is wired only for amountInput (D-04).
    - The scroll listener uses `{ passive: true }` and rAF as a per-frame coalescer with a comment distinguishing it from time-throttling (Pitfall 6).
  </acceptance_criteria>
  <done>Blur-incomplete (empty-value-gated, amountInput-only) and scroll-reversal (40% depth + hysteresis delta) both fire correctly; SIG-02 and SIG-03 tests green; SIG-05 firewall preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host-page DOM events → signal.js handlers | touch/blur/scroll events carry element references; handlers may read geometry/timing and (for blur) an emptiness boolean, never field-value strings |
| signal.js handlers → buildPayload → bus | the only egress of captured data; buildPayload's allow-list is the enforcement point |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | buildPayload / blur handler (SIG-05) | high | mitigate | Structural separation: blur handler reads el.value only into a boolean gate; buildPayload never receives or reads a value. Enforced by the SIG-05 allow-list test (green in Task 1) and a region-scoped source review of buildPayload |
| T-02-03 | Information Disclosure | internal bus traffic | low | accept | signal.js only calls the existing private-EventTarget publish() (Phase 1); it never constructs its own CustomEvent nor binds a host-page global — inherited Phase-1 mitigation, unchanged |
| T-02-SC | Tampering | npm installs | low | accept | No new packages installed this phase (RESEARCH.md Package Legitimacy Audit: N/A) — no install task to gate |
</threat_model>

<verification>
- `npx vitest run tests/signal.test.js -t "SIG-01"` / `-t "SIG-02"` / `-t "SIG-03"` / `-t "SIG-05"` all green.
- SIG-04 and SIG-06 remain RED (implemented in Plan 02-03) — expected mid-phase.
- Source review: buildPayload body free of PII-shaped reads; touch wiring CTA-scoped; blur wiring amountInput-only.
</verification>

<success_criteria>
- Touch hesitation (D-01/D-02), blur incomplete (D-03/D-04), and scroll reversal (D-05) fire per spec.
- buildPayload is the single PII-safe choke point (SIG-05) with D-07's payload shapes encoded.
- SIG-01/02/03/05 tests green.
</success_criteria>

<output>
Create `.planning/phases/02-signal-capture-layer/02-02-SUMMARY.md` when done.
</output>
