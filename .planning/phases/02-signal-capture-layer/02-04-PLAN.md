---
phase: 02-signal-capture-layer
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - test-harness/index.html
  - playwright.config.js
  - tests/e2e/harness.spec.js
autonomous: false
requirements: [SIG-01, SIG-02, SIG-03, SIG-04, SIG-05]

must_haves:
  truths:
    - "The debug-panel buttons dispatch real DOM events (fake touchstart/touchend sequences, focus/blur, scroll, popstate) that drive signal.js's real listeners, instead of calling window.Heed.publish() directly (D-08)"
    - "test-harness/index.html has enough scrollable height that document.documentElement.scrollHeight exceeds window.innerHeight * 1.5, so a real/simulated scroll can cross the 40% depth threshold (Pitfall 7)"
    - "A Playwright test opens the built harness in a real browser, clicks each rewired button, and asserts the #log panel shows exactly one PII-free receipt per signal type (D-08 real-browser fidelity)"
    - "A human confirms in a real/emulated mobile viewport that press-and-hold, blur-without-typing, scroll-down-then-up, and back-intent each produce exactly one PII-free receipt, closing Phase 2's gate"
  artifacts:
    - test-harness/index.html
    - playwright.config.js
    - tests/e2e/harness.spec.js
  key_links:
    - "test-harness/index.html loads dist/sdk.js (rebuilt via npm run build from src/index.js, which now wires signal.js per Plan 02-03) and calls window.Heed.initDemo() — the rewired buttons exercise that real capture path"
    - "the #log subscriber wiring (window.Heed.subscribe('signal:detected', appendLog)) stays byte-identical — only the trigger-button handler bodies change (D-08)"
---

<objective>
Rewire Phase 1's synthetic debug-panel buttons to dispatch real DOM events (D-08) so the harness
exercises signal.js's actual capture path rather than bypassing it via direct bus.publish(); add
scroll-height filler so scroll gestures can cross the 40% threshold (Pitfall 7); prove the rewiring
in a real browser with the project's first Playwright test; and close the phase with a human
mobile-viewport gate, mirroring Phase 1's 01-05-PLAN.md precedent.

Purpose: D-08 closes the loop between the debug panel and the real listeners built in Plans 02-02/
02-03, and the human gate is this project's `human_verify_mode: end-of-phase` checkpoint — a single
consolidated real-browser confirmation at the end of the phase rather than scattered mid-phase gates.
Output: rewired test-harness/index.html, playwright.config.js, tests/e2e/harness.spec.js, and a
human-verify sign-off that closes Phase 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signal-capture-layer/02-CONTEXT.md
@.planning/phases/02-signal-capture-layer/02-RESEARCH.md
@.planning/phases/02-signal-capture-layer/02-PATTERNS.md
@.planning/phases/02-signal-capture-layer/02-VALIDATION.md
@test-harness/index.html
@package.json
@CONTRACT.md
</context>

## Artifacts this plan produces

- `test-harness/index.html` (MODIFIED) — debug-panel button handlers rewired to dispatch real events (`simulateHold`, `simulateBlurIncomplete`, `simulateScrollReversal`, `simulateBackIntent`); scroll-height filler content added; the `#log` subscriber wiring and all 7 `data-heed` elements kept byte-identical
- `playwright.config.js` (NEW) — first Playwright config in the project; mobile touch-enabled viewport, no `webServer` block (tests navigate directly via a `file://` URL, matching the harness's no-server design)
- `tests/e2e/harness.spec.js` (NEW) — first Playwright E2E test; clicks the rewired buttons and asserts `#log` receipts for all four signal types

<tasks>

<task type="auto">
  <name>Task 1: Rewire debug-panel buttons to dispatch real DOM events and add scroll-height filler (D-08, Pitfall 7)</name>
  <files>test-harness/index.html</files>
  <read_first>
    - test-harness/index.html (the file being modified — full current content, especially the debug-panel `<script>` block and its four button handlers)
    - .planning/phases/02-signal-capture-layer/02-RESEARCH.md (Code Examples: "Debug-Panel Rewiring to Dispatch Real Events"; the isTrusted fidelity note; Pitfall 7)
    - .planning/phases/02-signal-capture-layer/02-PATTERNS.md (section "test-harness/index.html (modified)" — keep the #log subscriber, replace only handler bodies, comment-style convention)
  </read_first>
  <action>
    Replace the four debug-panel trigger-button click handlers so they dispatch real DOM events onto
    the mapped data-heed elements instead of calling window.Heed.publish() directly. Add
    simulateHold(el, holdMs) that constructs a real Touch and dispatches touchstart then, after
    holdMs (use a value past the 800ms threshold, e.g. 900ms via setTimeout), dispatches touchend —
    wired to the touch_hesitation button, targeting the proceed-cta element. Add
    simulateBlurIncomplete(el) dispatching a real focus FocusEvent, ensuring el.value is empty, then
    dispatching a blur FocusEvent — wired to the blur_incomplete button, targeting amount-input. Add
    simulateScrollReversal() that calls window.scrollTo to move past innerHeight*0.5 (past the 40%
    depth threshold), dispatches a scroll Event, then calls window.scrollTo to move back by more than
    the configured minimum reversal delta (over 50px) and dispatches a second scroll Event — wired to
    the scroll_reversal button. Add simulateBackIntent() dispatching a real
    PopStateEvent('popstate', { state: null }) on window — wired to the back_intent button. Keep the
    window.Heed.subscribe('signal:detected', appendLog) wiring exactly as-is (already proves receipt
    via the bus, decoupled from triggers). Update the debug-panel header comment (currently "synthetic
    triggers only ... NOT real touch/scroll/blur/popstate detection") to state D-08's new fidelity:
    buttons now dispatch real (isTrusted:false) DOM events that drive signal.js's real listeners, and
    note this still is not a substitute for Phase 6's real-device manual sequence. Add scroll-height
    filler content (a spacer element or additional vertical padding across the existing screens) so
    document.documentElement.scrollHeight exceeds window.innerHeight * 1.5 on a mobile viewport
    (Pitfall 7) — otherwise the scroll_reversal button silently no-ops with nothing to scroll. Do NOT
    rename, remove, or reorder any of the 7 locked data-heed elements (CONTRACT.md).
  </action>
  <verify>
    <automated>grep -q "new TouchEvent" test-harness/index.html && grep -q "new PopStateEvent" test-harness/index.html && grep -q "new FocusEvent" test-harness/index.html && grep -q "window.Heed.subscribe" test-harness/index.html && echo "D-08 rewiring present"</automated>
  </verify>
  <acceptance_criteria>
    - The command in `<verify>` prints `D-08 rewiring present`.
    - None of the four debug-panel button click handlers call `window.Heed.publish(` directly anymore (grep for `publish(` inside the `.debug-panel button` click-handler bodies returns no match; the only remaining `publish` reference in the file is inside `src`/`dist`-independent library code, not this script block).
    - `document.querySelectorAll('[data-heed]').length` in the modified file still resolves to exactly 7 elements with unchanged selector values.
    - The harness's total document height exceeds `window.innerHeight * 1.5` (verified by a spacer/padding addition — confirm by reading the diff, no dedicated automated height-measurement command exists at this static-file stage; Task 2's Playwright test performs the runtime confirmation).
  </acceptance_criteria>
  <done>Debug-panel buttons dispatch real DOM events that exercise signal.js's actual listeners (D-08); scroll filler added so the 40% threshold is genuinely crossable; the #log subscriber and all 7 selectors remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Playwright config and author the D-08 real-browser E2E spec</name>
  <files>playwright.config.js, tests/e2e/harness.spec.js</files>
  <read_first>
    - package.json (confirms @playwright/test is already a devDependency, no config exists yet)
    - test-harness/index.html (the rewired file from Task 1 — button/selector structure the spec drives)
    - .planning/phases/02-signal-capture-layer/02-RESEARCH.md ("happy-dom vs. Playwright Test Split" section — build dist/sdk.js first, open the real harness HTML, click buttons, assert #log content)
    - .planning/phases/02-signal-capture-layer/02-PATTERNS.md ("tests/e2e/harness.spec.js" — no analog exists; playwright.config.js is an implicit prerequisite)
  </read_first>
  <action>
    Create playwright.config.js at the repo root using defineConfig from '@playwright/test': set
    testDir to './tests/e2e', reporter to 'list', and a use block setting viewport to
    { width: 390, height: 844 } (matches STACK.md's 390px iOS target), hasTouch: true, and
    isMobile: true. Do NOT add a webServer block — the spec file navigates directly via a file://
    URL, consistent with the harness's no-server, no-Branch-1 design (same precedent as Phase 1's
    01-05-PLAN.md manual "double-click the file" verification). Create tests/e2e/harness.spec.js
    importing { test, expect } from '@playwright/test' and Node's path module. In a
    test.beforeEach, navigate via page.goto with a file:// URL built from
    path.resolve('test-harness/index.html'). Write one test per signal type: click the
    touch_hesitation debug button and assert (with Playwright's auto-retrying expect, default
    timeout is enough to cover the ~900ms hold) that #log's text content contains a
    "touch_hesitation" entry; click blur_incomplete and assert a "blur_incomplete" entry appears;
    click scroll_reversal and assert a "scroll_reversal" entry appears; click back_intent and
    assert a "back_intent" entry appears. For at least one of these tests, parse the last JSON line
    out of #log's textContent and assert Object.keys(payload).sort() matches that signal type's
    allow-list exactly (mirrors the SIG-05 unit-test assertion at the real-browser layer, e.g. for
    back_intent: type, targetSelector, bbox, pathname, timestamp per D-07). Note in a comment that
    `npm run build` must run before this suite (dist/sdk.js must reflect the latest src/signal.js
    and src/index.js wiring from Plans 02-02/02-03) — do not attempt to auto-invoke the build from
    within the spec file; that is the responsibility of the run command.
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/e2e/harness.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build && npx playwright test tests/e2e/harness.spec.js` exits 0.
    - playwright.config.js sets `testDir: './tests/e2e'`, `hasTouch: true`, `isMobile: true`, and a 390-wide viewport; it contains no `webServer` block.
    - tests/e2e/harness.spec.js contains four assertions (or four tests), one per signal type, each confirming a corresponding `#log` entry appears after clicking its rewired button.
    - At least one test asserts the parsed payload's key set matches its signal type's exact allow-list (no PII-shaped extra key).
  </acceptance_criteria>
  <done>playwright.config.js and tests/e2e/harness.spec.js exist; running the built harness in a real headless browser proves D-08's rewiring drives signal.js's real listeners for all four signal types with PII-free payloads.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the rewired harness end-to-end in a real/emulated mobile browser (Phase 2 gate)</name>
  <action>Pause for human verification — the operator builds the SDK, confirms the full automated suite and Playwright suite are green, then opens test-harness/index.html in a real/emulated mobile browser and confirms the harness behaves as specified per the how-to-verify steps below. No files are modified by this task; it is a blocking gate that closes Phase 2.</action>
  <what-built>
    signal.js now captures all 4 signal types (touch hesitation, blur incomplete, scroll reversal,
    back intent) from real DOM events, gated by CTA-only scope (D-02), a final-value blur diff
    (D-03), a scroll hysteresis delta (D-05), and a cached flowComplete flag (D-06); it survives 3+
    simulated SPA navigations without double-firing or silent under-attachment (SIG-06); every
    payload is PII-free (SIG-05). The test-harness debug panel now dispatches real DOM events
    (D-08) instead of calling the bus directly, and a Playwright suite already proves this in a
    headless real browser.
  </what-built>
  <how-to-verify>
    1. Run `npm run build` (produces a current dist/sdk.js).
    2. Confirm the full automated suite is green: `npx vitest run` — all SIG-01 through SIG-06 tests pass.
    3. Confirm the Playwright suite is green: `npx playwright test`.
    4. Open test-harness/index.html directly in a browser (double-click the file / file:// URL — no server, no Branch 1). In Chrome DevTools, open the device toolbar, select a 390px-wide mobile preset (e.g. iPhone), and enable touch simulation.
    5. Press and hold the "Proceed" button (data-heed="proceed-cta") for about 1 second using a real click-and-hold — confirm exactly one `touch_hesitation` entry appears in the on-page log with a payload containing only type/targetSelector/bbox/timestamp.
    6. Tap into the amount field, then tap away without typing anything — confirm exactly one `blur_incomplete` entry appears. Then tap into the amount field again, type a value, and tap away — confirm NO new entry appears (D-03's final-value-diff negative case).
    7. Scroll down past roughly 40% of the page height, then scroll back up by a deliberate amount — confirm exactly one `scroll_reversal` entry appears with a `scrollDepth` field (a small jittery bounce should NOT produce an entry).
    8. Click the "Simulate back_intent" debug-panel button (the harness has no client-side router, so a real browser-back has no meaningful history entry to test against here) — confirm exactly one `back_intent` entry appears with a `pathname` field.
    9. Review every logged entry from steps 5-8: confirm each contains ONLY the allow-listed keys for its type — no field values, no `value`/`text` content, no cookies, no localStorage content ever appear.
  </how-to-verify>
  <resume-signal>Type "approved" if all four signal types produce exactly one PII-free bus receipt each (via real press/tap/scroll interaction where applicable, and via the debug panel for back_intent) and the D-03/D-05 negative cases correctly produce no entry; otherwise describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| debug-panel dispatched events → signal.js listeners | Real (if programmatically constructed, isTrusted:false) DOM events now drive the production listener code path, not a bypass — the same PII-safety guarantees from Plans 02-02/02-03 must hold under real dispatch |
| human verifier → harness runtime | Manual confirmation, in a real/emulated mobile browser, that the shipped harness behaves as specified — the project's `human_verify_mode: end-of-phase` gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | logged real-browser payloads (D-08, SIG-05) | high | mitigate | The Playwright spec (Task 2) and the human-verify checklist (Task 3, step 9) both explicitly re-check the payload allow-list at the real-DOM-dispatch layer, not just the happy-dom unit-test layer — a second, independent enforcement of the same SIG-05 invariant under real event dispatch |
| T-02-06 | Information Disclosure | human-verify backstop | high | mitigate | Mirrors Phase 1's T-01-06 pattern — the human-verify step explicitly checks that every logged payload contains only its allow-listed fields, no field values, cookies, or localStorage, as the final manual backstop on the No-PII rule before the phase gate closes |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed this phase — @playwright/test was already a verified devDependency as of 01-RESEARCH.md's Package Legitimacy Audit; no install task exists here to gate |
</threat_model>

<verification>
- `npm run build && npx vitest run && npx playwright test` all green.
- Debug-panel buttons dispatch real DOM events (grep-verified); #log subscriber and 7 selectors unchanged.
- Human-verify checkpoint approved: all 4 signal types produce exactly one PII-free receipt in a real/emulated mobile browser; D-03/D-05 negative cases produce no entry.
</verification>

<success_criteria>
- D-08's debug-panel rewiring is real (not synthetic bus.publish() calls) and proven in a headless real browser via Playwright.
- test-harness/index.html has genuine scrollable height for the 40% threshold (Pitfall 7).
- Phase 2 gate closed: human confirms all 4 signal types fire correctly and PII-free in a real mobile-viewport browser.
</success_criteria>

<output>
Create `.planning/phases/02-signal-capture-layer/02-04-SUMMARY.md` when done.
</output>
