---
phase: 06-integration-verification-against-live-branch-1
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - playwright.config.js
  - tests/e2e/branch1-live.spec.js
autonomous: false
requirements: [INTEG-01]
user_setup: []

must_haves:
  truths:
    - "Press-and-hold on live /swap's proceed-cta past 800ms produces a touch_hesitation signal; blurring an untouched amount-input produces blur_incomplete; scrolling down then up past 40% produces scroll_reversal (SC1a/b/c)"
    - "Real browser-back navigation (page.goBack()) on /confirm — NOT a tap on the on-screen back-btn — produces back_intent (SC1d, Pitfall 1)"
    - "The console log order for a triggered signal is exactly signal_detected -> inference_run -> response_fired with no missing or reordered steps (SC2)"
    - "The response overlay renders above Branch 1's UI: [data-heed-overlay] container is pointer-events:none, [data-heed-response] bubble is pointer-events:auto, and the underlying proceed-cta is still clickable through the overlay (SC3)"
    - "No [heed] log entries fire while on / (Screen 1, excluded from activeScreens) (SC4)"
    - "The full spec 8-step manual testing sequence passes against the live worktree'd Branch 1 (human-verify), AND the live Playwright suite is green (D-06 both)"
  artifacts:
    - tests/e2e/branch1-live.spec.js
    - playwright.config.js
  key_links:
    - "branch1-live.spec.js back_intent test -> page.goBack() (NOT clicking back-btn — Pitfall 1)"
    - "branch1-live.spec.js -> page.on('console') array-order assertion for SC2"
    - "playwright.config.js projects[live-branch1].use.baseURL -> http://localhost:3000 (worktree's next dev)"
    - "each test -> waitForFunction(() => window.__heedReady === true) (boot hook from Plan 06-01 Task 3)"
---

<objective>
Verify the already-built SDK behaves correctly against a live Branch 1, satisfying INTEG-01's
four success criteria — the "verify" half of D-07's two-plan split. Both an automated
Playwright regression suite AND a human-run manual walkthrough (D-06, both not either/or).

Purpose: Turn the four roadmap success criteria into a repeatable regression gate (Playwright
project pointed at the worktree's `next dev`), and run the spec's own 8-step manual testing
sequence by hand to catch real-device/visual/positioning issues automation misses. This plan
adds no SDK logic — it exercises Plan 06-01's wiring.

Output: A `live-branch1` Playwright project in `playwright.config.js`, a new
`tests/e2e/branch1-live.spec.js` covering SC1-SC4, and a human-verify phase gate that closes
Phase 6 (and discards the worktree's uncommitted script-tag edit).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-verification-against-live-branch-1/06-CONTEXT.md
@.planning/phases/06-integration-verification-against-live-branch-1/06-RESEARCH.md
@.planning/phases/06-integration-verification-against-live-branch-1/06-PATTERNS.md
@.planning/phases/06-integration-verification-against-live-branch-1/06-VALIDATION.md

# Files this plan reads/modifies
@playwright.config.js
@tests/e2e/harness.spec.js
@.planning/phases/06-integration-verification-against-live-branch-1/06-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 06-02)

New files:
- `tests/e2e/branch1-live.spec.js` — Playwright e2e suite covering INTEG-01 SC1 (touch_hesitation, blur_incomplete, scroll_reversal, back_intent), SC2 (console log order), SC3 (overlay non-blocking), SC4 (no logs on Screen 1). Runs against `http://localhost:3000` (worktree `next dev`).

New symbols / config added to existing files:
- `playwright.config.js`:
  - `projects` array with two entries: `file-harness` (`testMatch: 'harness.spec.js'`, keeps existing file:// behavior) and `live-branch1` (`testMatch: 'branch1-live.spec.js'`, `use: { baseURL: 'http://localhost:3000' }`).

Verification outcome (not a file):
- Human-verify phase gate sign-off (D-06 manual walkthrough + green live Playwright suite), plus discard of the worktree's uncommitted `app/layout.tsx` edit (D-02 cleanup).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add a live-branch1 Playwright project pointed at the worktree's next dev server</name>
  <files>playwright.config.js</files>
  <read_first>
    - playwright.config.js (the full existing 17-line file — extend in place, do not replace; current top-level use block has the 390px mobile viewport + hasTouch + isMobile)
    - .planning/phases/06-integration-verification-against-live-branch-1/06-PATTERNS.md (playwright.config.js edit-in-place pattern; second-project shape)
    - .planning/phases/06-integration-verification-against-live-branch-1/06-RESEARCH.md (Pattern 3: second Playwright project, no webServer block — servers launched manually per D-06)
  </read_first>
  <action>
    Extend `playwright.config.js` in place: keep the existing top-level `use` block (390px viewport, `hasTouch: true`, `isMobile: true`) as shared defaults, and add a `projects` array with two entries. First entry: `{ name: 'file-harness', testMatch: 'harness.spec.js' }` (preserves the existing file:// harness suite's behavior). Second entry: `{ name: 'live-branch1', testMatch: 'branch1-live.spec.js', use: { baseURL: 'http://localhost:3000' } }` so the new spec navigates relative to the worktree's `next dev`. Add NO `webServer` block to either project — both dev servers (`npm run receiver` on :4310 and the worktree's `npm run dev` on :3000) are started manually per D-06's manual-walkthrough framing, matching this repo's existing no-auto-launch convention. Update the file's header comment to note the second project targets the live worktree'd Branch 1.
  </action>
  <verify>
    <automated>npx playwright test --list 2>&1 | grep -q 'live-branch1' && npx playwright test --list 2>&1 | grep -q 'file-harness'</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.js` exports a `projects` array with a `file-harness` entry (`testMatch: 'harness.spec.js'`) and a `live-branch1` entry (`testMatch: 'branch1-live.spec.js'`, `use.baseURL: 'http://localhost:3000'`).
    - `npx playwright test --list` reports both projects and does not error on config parse.
    - No `webServer` block exists in the config (servers are manual).
    - The existing `file-harness` behavior (390px, hasTouch, isMobile from top-level `use`) is preserved.
  </acceptance_criteria>
  <done>playwright.config.js has both projects; the live-branch1 project targets http://localhost:3000 with no webServer auto-launch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author tests/e2e/branch1-live.spec.js covering INTEG-01 SC1-SC4</name>
  <files>tests/e2e/branch1-live.spec.js</files>
  <read_first>
    - tests/e2e/harness.spec.js (the analog suite — same [heed] assertion role; reuse the beforeEach waitForFunction(__heedReady) idiom and the back_intent payload allow-list assertion at lines 48-69; overlay/pointer-events assertions)
    - .planning/phases/06-integration-verification-against-live-branch-1/06-RESEARCH.md (Code Examples: branch1-live.spec.js sketch; Pitfall 1 page.goBack() not back-btn; Pitfall 2; Validation Architecture per-SC test map)
    - .planning/phases/06-integration-verification-against-live-branch-1/06-PATTERNS.md (branch1-live.spec.js pattern: page.on('console') collection; overlay assertions; SC4 no-signal pattern; __heedReady per-test wait)
    - .planning/phases/06-integration-verification-against-live-branch-1/06-VALIDATION.md (per-task verification map: exact -g test names SC1a/b/c/d, SC2, SC3, SC4)
    - config/demo-platform-live.json (activeScreens the SC4 gating depends on)
  </read_first>
  <behavior>
    - SC4: navigate to `/` (Screen 1), wait ~1s, assert NO console line contains `[heed]` (Screen 1 not in activeScreens).
    - SC1a touch_hesitation: on `/swap`, press-and-hold `[data-heed="proceed-cta"]` past the 800ms threshold via a real touch sequence (touchstart, wait >800ms, touchend), assert a console line contains `"type":"touch_hesitation"`.
    - SC1b blur_incomplete: on `/swap`, focus then blur `[data-heed="amount-input"]` with no value change, assert `"type":"blur_incomplete"`.
    - SC1c scroll_reversal: on `/swap`, scroll down past 40% viewport depth then reverse upward (delta > minReversalDeltaPx), assert `"type":"scroll_reversal"`.
    - SC1d back_intent: reach `/confirm` (via /swap → proceed), then call `page.goBack()` (real browser back → fires popstate). Assert `"type":"back_intent"`. Assert the payload key set equals the D-07 allow-list `['bbox','pathname','targetSelector','timestamp','type']` (reuse harness.spec.js's structural assertion). MUST NOT click `[data-heed="back-btn"]` (router.push does not fire popstate — Pitfall 1).
    - SC2: for a triggered signal, collect `page.on('console')` into an array, filter `[heed]`-prefixed lines, parse each entry's `event`, filter to `['signal_detected','inference_run','response_fired']`, assert the resulting order equals exactly `['signal_detected','inference_run','response_fired']`.
    - SC3: after a signal fires and a response renders, assert `[data-heed-overlay]` has CSS `pointer-events: none`, `[data-heed-response]` has CSS `pointer-events: auto`, and `[data-heed="proceed-cta"]` is still `.click()`-able through the overlay.
  </behavior>
  <action>
    Create `tests/e2e/branch1-live.spec.js`, adapting `tests/e2e/harness.spec.js`'s idioms to a live target. Navigation is relative to the `live-branch1` project's `baseURL` (`await page.goto('/')`, `'/swap'`, `'/confirm'`) — no `HARNESS_URL`/`path` import. Each test that expects signals must `await page.waitForFunction(() => window.__heedReady === true)` after navigating (the boot hook set in Plan 06-01 Task 3) before dispatching events, so `src/signal.js` listeners are guaranteed attached first. Collect logs via `page.on('console', (msg) => logs.push(msg.text()))` registered before the triggering action. Name the tests so the VALIDATION.md `-g` filters resolve: include the substrings `touch_hesitation`, `blur_incomplete`, `scroll_reversal`, `back_intent`, `SC2`, `SC3`, `SC4` in the relevant test titles. For back_intent, use `page.goBack()` — never a click on `back-btn` (Pitfall 1). For the touch-hold, drive a real >800ms press via Playwright's touch APIs / dispatched touch events (hasTouch is already set globally). Add a top-of-file header comment noting: (a) both dev servers must be running, (b) the back_intent Pitfall 1 rationale, and (c) that this suite exercises Plan 06-01's wiring against live Branch 1.

    Note: the live green run requires both dev servers up (receiver :4310 + worktree next dev :3000), which happens at the Task 3 checkpoint. This task's automated verify only confirms the spec is syntactically valid and discovered by the runner (`--list`), so authoring is checkable without servers.
  </action>
  <verify>
    <automated>npx playwright test --project=live-branch1 --list 2>&1 | grep -Eq 'touch_hesitation|blur_incomplete|scroll_reversal|back_intent' && node -e "const s=require('fs').readFileSync('tests/e2e/branch1-live.spec.js','utf8');if(!s.includes('page.goBack('))throw new Error('back_intent must use page.goBack()');if(/click\(\s*['\"]\[data-heed=\\\"back-btn\\\"\]/.test(s))throw new Error('must NOT click back-btn (Pitfall 1)');if(s.includes('initDemo'))throw new Error('must not reference initDemo');console.log('spec OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/branch1-live.spec.js` exists and `npx playwright test --project=live-branch1 --list` discovers tests titled for `touch_hesitation`, `blur_incomplete`, `scroll_reversal`, `back_intent`, `SC2`, `SC3`, `SC4`.
    - The back_intent test uses `page.goBack()` and does NOT click `[data-heed="back-btn"]` (source assertion in the grep gate).
    - The SC2 test collects `page.on('console')` output and asserts the exact order `['signal_detected','inference_run','response_fired']`.
    - The SC3 test asserts `[data-heed-overlay]` pointer-events:none, `[data-heed-response]` pointer-events:auto, and proceed-cta still clickable.
    - The SC4 test asserts no `[heed]` log fires on `/`.
    - With both dev servers running, `npx playwright test --project=live-branch1` passes all tests green (confirmed at Task 3's checkpoint).
  </acceptance_criteria>
  <done>The live Playwright spec covers all four success criteria, uses real back navigation for back_intent, and is discovered by the runner; it goes green once both servers are up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify phase gate — manual walkthrough + live Playwright suite (D-06 both)</name>
  <action>Pause for human verification — the operator starts both dev servers (receiver :4310 + worktree next dev :3000), runs the live Playwright suite green, walks the spec's 8-step manual sequence against the live Branch 1, and discards the worktree's uncommitted layout.tsx edit. No files in this repo are modified; this is the blocking Phase 6 gate. Follow the how-to-verify steps below.</action>
  <what-built>
    The full Phase 6 integration wiring (Plan 06-01) plus the live Playwright regression suite
    (Task 2). This checkpoint runs D-06's required BOTH: the spec's 8-step manual testing
    sequence by hand, AND the automated live suite — against a live worktree'd Branch 1.
  </what-built>
  <how-to-verify>
    1. Start the receiver: `npm run receiver` (serves :4310 — /weights, /sdk.js, /config/demo-platform-live.json). Confirm it logs `listening on http://localhost:4310`.
    2. In a second terminal, start Branch 1: `cd ../heed-worktree-demo-platform && npm run dev` (plain `next dev`, localhost :3000 — NOT `dev:network`, Pitfall 4). Wait for it to report ready on http://localhost:3000.
    3. Automated gate: from this repo root, run `npx playwright test --project=live-branch1`. All SC1-SC4 tests must pass green. Then run the full suite `npx playwright test` (both `file-harness` and `live-branch1` projects) and confirm no regressions.
    4. Manual walkthrough on a real 390px browser at http://localhost:3000:
       a. Open `/` (Screen 1) — confirm NO `[heed]` lines appear in the console (SC4).
       b. Go to `/swap`. Press-and-hold `proceed-cta` >800ms — confirm a `touch_hesitation` receipt (SC1a).
       c. On `/swap`, tap into `amount-input` then tap away without typing — confirm `blur_incomplete` (SC1b).
       d. On `/swap`, scroll down past ~40% then scroll back up — confirm `scroll_reversal` (SC1c).
       e. Proceed to `/confirm`, then use the BROWSER'S OWN back button / back gesture (NOT the on-screen back-btn — Pitfall 1) — confirm `back_intent` (SC1d).
       f. For a triggered signal, confirm the console shows `signal_detected → inference_run → response_fired` in order (SC2).
       g. Confirm the overlay renders visually ABOVE Branch 1's UI and you can still tap the underlying `proceed-cta` through it (SC3).
    5. Cleanup (D-02, CLAUDE.md hard rule — MANDATORY before sign-off): discard the worktree's uncommitted script-tag edit. Run `git -C ../heed-worktree-demo-platform checkout -- app/layout.tsx` (or remove the worktree entirely with `git worktree remove ../heed-worktree-demo-platform`). Confirm `git -C ../heed-worktree-demo-platform status --porcelain` is clean and nothing was ever committed on `feat/demo-platform`.
  </how-to-verify>
  <resume-signal>
    Type "approved" once both the automated live suite is green AND all 8 manual steps pass AND
    the worktree edit has been discarded (feat/demo-platform history untouched). Otherwise
    describe which criterion failed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright runner → worktree page (`http://localhost:3000`) | Test automation drives the live Browser/Client tier from outside; not part of the shipped architecture |
| worktree working tree → `feat/demo-platform` git history | The uncommitted `app/layout.tsx` edit (from Plan 06-01) must be discarded here, never committed (CLAUDE.md "No cross-branch contamination") |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering (of Branch 1's real git history) | Uncommitted `app/layout.tsx` script-tag edit inside the `feat/demo-platform` worktree (carried from Plan 06-01) | high | mitigate | Cleanup is enforced as a MANDATORY step in this plan's human-verify checkpoint (step 5): `git checkout -- app/layout.tsx` or `git worktree remove` before sign-off; confirm `feat/demo-platform` history is untouched. Blocks per `security_block_on: high` — a commit here violates the CLAUDE.md hard rule. |
| T-06-04 | Information Disclosure | Worktree `next dev` binding | low | accept | Actionable note in checkpoint step 2: run plain `next dev` (localhost-only), never Branch 1's `dev:network` (`-H 0.0.0.0`) script (Pitfall 4). No code change. |
| T-06-05 | Denial of Service (false gate pass) | SC4 gating correctness | medium | mitigate | If the boot script had called `initDemo()` (Pitfall 2), the permissive bundled config (`activeScreens: []`) would make SC4 trivially/falsely pass on every screen. Mitigated upstream (Plan 06-01 Task 3 asserts `init()` not `initDemo()`); this plan's SC4 test + manual step (a) confirm gating actually excludes `/`. |
</threat_model>

<verification>
- Automated live suite green: with both servers running, `npx playwright test --project=live-branch1` passes all SC1-SC4 tests; `npx playwright test` (full suite) has no regressions in `file-harness`.
- back_intent uses real navigation: source grep confirms `page.goBack()` and absence of a `back-btn` click (Pitfall 1).
- SC2 order asserted from `page.on('console')` array: `['signal_detected','inference_run','response_fired']`.
- SC3 overlay assertions: container pointer-events:none, bubble pointer-events:auto, underlying CTA clickable.
- SC4: no `[heed]` logs on `/`.
- Manual walkthrough sign-off recorded (D-06 both).
- Cross-branch cleanup confirmed: worktree edit discarded, `feat/demo-platform` history untouched.
</verification>

<success_criteria>
- INTEG-01 satisfied: SC1 (all 4 signals on live Branch 1), SC2 (log order), SC3 (non-blocking overlay), SC4 (no logs on Screen 1) all pass — automated AND manual (D-06).
- `tests/e2e/branch1-live.spec.js` is a repeatable regression gate under the `live-branch1` project.
- The worktree's uncommitted script-tag edit is discarded; `feat/demo-platform` history is untouched.
- Phase 6 gate closed.
</success_criteria>

<output>
Create `.planning/phases/06-integration-verification-against-live-branch-1/06-02-SUMMARY.md` when done.
</output>
