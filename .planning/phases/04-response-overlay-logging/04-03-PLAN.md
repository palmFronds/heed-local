---
phase: 04-response-overlay-logging
plan: 03
type: execute
wave: 1
depends_on: ["04-01", "04-02"]
files_modified:
  - src/signal.js
  - src/log.js
autonomous: true
requirements: [LOG-01]
must_haves:
  truths:
    - "Each of the 6 pipeline events produces exactly one console.log('[heed]', JSON.stringify(entry)) line with { ts, sessionId, partnerId, event, data }."
    - "console.log('[heed]', ...) is called from exactly one place in the codebase — log.js's writeLog choke point."
    - "flow:complete and pagehide together call endSession exactly once (sessionEnded guard), never twice."
    - "No log line fires while window.location.pathname is outside config.activeScreens (live-read, never cached)."
    - "checkFlowComplete publishes flow:complete exactly once, at the false→true transition of flowCompleteFlag."
  artifacts:
    - "src/log.js real implementation: writeLog choke point, isActiveScreen live gate, initLogging session-lifecycle wiring"
    - "src/signal.js checkFlowComplete extended with one publish('flow:complete', {}) call"
  key_links:
    - "signal.js's flow:complete publish is the sole trigger for the flow_complete log line + endSession(true)"
    - "the sessionEnded guard is the single point preventing endSession's non-idempotent double-count"
    - "isActiveScreen's live pathname read is the gate that keeps logs off Screen 1 (INTEG-01 alignment)"
---

<objective>
Implement `src/log.js`'s real behavior — the structured-logging choke point (LOG-01), the `activeScreens` gate (D-06/D-07), and sole ownership of session-lifecycle wiring (D-01/D-02/D-03: subscribe `flow:complete`, register a `pagehide` listener, guard `endSession` with a once-per-session `sessionEnded` boolean) — and extend `src/signal.js`'s `checkFlowComplete()` with the one `publish('flow:complete', {})` call D-01 requires.

Purpose: log.js is the SOLE caller of `console.log('[heed]', ...)` in the codebase (locked by UI-SPEC) and the single place session lifecycle is wired. Co-locating the endSession trigger here (not index.js) avoids a second subscription to flow:complete, since log.js already subscribes to log the event (04-RESEARCH.md A1).
Output: A fully implemented log.js and a one-line extension to signal.js's checkFlowComplete, turning the RED log suite GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-response-overlay-logging/04-RESEARCH.md
@.planning/phases/04-response-overlay-logging/04-UI-SPEC.md
@.planning/phases/04-response-overlay-logging/04-PATTERNS.md
@src/inference.js
@src/signal.js
@src/bus.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement src/log.js — writeLog choke point, isActiveScreen gate, and all 6 event subscriptions</name>
  <files>src/log.js</files>
  <read_first>
    - src/log.js (the Plan 04-02 stub — replace stub bodies with real impl, keep the exact export names)
    - tests/log.test.js (the RED suite this task must turn GREEN — read the exact assertions for entry shape and per-event data fields)
    - src/inference.js (inference:result publish shape lines 266-277 — the fields log.js reads for inference_run data; endSession signature + non-idempotency doc lines 281-304)
    - src/signal.js (signal:detected payload shape; maybeReattach live-pathname-read lines ~297-305 — the idiom isActiveScreen must copy)
    - .planning/phases/04-response-overlay-logging/04-RESEARCH.md (Pattern 3 lines 330-376 — initLogging/finishSession/isActiveScreen/writeLog verbatim skeleton)
    - .planning/phases/04-response-overlay-logging/04-UI-SPEC.md (Logging Contract lines 152-166 — per-event data fields)
    - .planning/phases/04-response-overlay-logging/04-PATTERNS.md (src/log.js section — one-time-registration guard + reset-on-reinit + single-writeLog-choke-point discipline)
  </read_first>
  <behavior>
    - writeLog(config, sessionId, event, data): returns without logging if isActiveScreen(config) is false; otherwise emits exactly one console.log('[heed]', JSON.stringify({ ts: Date.now(), sessionId, partnerId: config.platformId, event, data })).
    - isActiveScreen(config): reads window.location.pathname FRESH on every call; returns true when config.activeScreens is absent/empty (permissive default); otherwise returns whether the list includes the live pathname.
    - initLogging subscribes signal:detected → writeLog 'signal_detected'; inference:result → writeLog 'inference_run' (regardless of fires); response:fired → 'response_fired'; response:dismissed → 'response_dismissed'; flow:complete → finishSession(true,'flow_complete'); pagehide → finishSession(false,'flow_abandoned') only if not already ended.
    - finishSession(outcome,event): no-op if sessionEnded; else set sessionEnded=true, writeLog the event, call endSession(config, outcome) — combined exactly-once across both lifecycle paths.
    - Repeat initLogging calls reset sessionEnded=false but never stack duplicate subscriptions/listeners (one-time-registration guard).
  </behavior>
  <action>
    Replace the src/log.js stub bodies with the real implementation, preserving the exported symbol names (initLogging, isActiveScreen, writeLog). Import `subscribe` from './bus.js' and `endSession` from './inference.js'. Add module-level `let sessionEnded = false;` (D-03 guard) and a `let initialized = false;` one-time-registration guard mirroring inference.js/signal.js. writeLog is the ONE function that calls console.log('[heed]', ...) — every event handler routes through it; never inline a console.log at a subscribe site (single choke-point discipline, locked by UI-SPEC). Gate writeLog on isActiveScreen(config) at its top (D-06). Build the entry as { ts: Date.now(), sessionId, partnerId: config.platformId, event, data } (D-09: partnerId from config.platformId). isActiveScreen reads window.location.pathname live on every call (never hoist/cache — RESEARCH Anti-Patterns; mirrors maybeReattach), returns true for absent/empty activeScreens. initLogging registers the 6 handlers (signal:detected, inference:result, response:fired, response:dismissed via subscribe; flow:complete via subscribe; pagehide via window.addEventListener) behind the initialized guard, resetting sessionEnded=false every call. finishSession implements the sessionEnded short-circuit then writeLog then endSession(config, outcome). For inference_run data include intent/confidence/fires from the inference:result payload; for response_fired/response_dismissed pass through the data from the response:fired/response:dismissed bus payloads; for flow_complete/flow_abandoned use an empty data object. Keep the No-PII firewall header comment — never read any element's value/textContent/innerHTML.
  </action>
  <verify>
    <automated>npx vitest run tests/log.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/log.test.js -t "LOG-01"`, `-t "session-lifecycle"`, and `-t "activeScreens"` are all GREEN.
    - Source assertion: exactly one occurrence of the `[heed]` console.log call site exists in src/log.js and none elsewhere (grep src excluding comments — `grep -rn "console.log" src/ | grep -v "^#" | grep heed` shows only log.js's writeLog).
    - Source assertion: src/log.js reads window.location.pathname inside isActiveScreen's body (not hoisted to module/init scope).
    - Source assertion: src/log.js declares a module-level sessionEnded guard and an initialized registration guard.
  </acceptance_criteria>
  <done>log.js emits one gated, correctly-shaped log line per event and calls endSession exactly once per session; the RED log suite is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Extend signal.js checkFlowComplete to publish flow:complete once at the false→true transition</name>
  <files>src/signal.js</files>
  <read_first>
    - src/signal.js (checkFlowComplete lines 240-247 — the exact `if (el && getComputedStyle(el).display !== 'none')` block where flowCompleteFlag is set true; publish is already imported at line 5)
    - .planning/phases/04-response-overlay-logging/04-PATTERNS.md (src/signal.js MODIFY section — "exactly one added line inside the branch that sets flowCompleteFlag = true")
    - tests/log.test.js (the session-lifecycle case that drives flow:complete)
  </read_first>
  <action>
    In src/signal.js checkFlowComplete, inside the existing `if (el && getComputedStyle(el).display !== 'none')` branch that sets `flowCompleteFlag = true`, add exactly one `publish('flow:complete', {})` call immediately after the assignment. Because the function's top guard `if (flowCompleteFlag) return;` already ensures the branch runs at most once, this publishes flow:complete exactly once at the false→true transition (D-01). `publish` is already imported at line 5 — add no new import. Do NOT restructure the guard-at-top shape or add a second detection path — this reuses Phase 2's already-verified completion detection (D-01 rationale).
  </action>
  <verify>
    <automated>npx vitest run tests/log.test.js tests/signal.test.js tests/signal-spa.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: src/signal.js checkFlowComplete contains a `publish('flow:complete'` call inside the flowCompleteFlag-set branch, and only there.
    - Behavior: the tests/log.test.js session-lifecycle case that publishes flow:complete drives finishSession(true,'flow_complete') exactly once.
    - Regression: tests/signal.test.js and tests/signal-spa.test.js stay GREEN (no double-fire, no guard restructure).
  </acceptance_criteria>
  <done>checkFlowComplete publishes flow:complete once at completion; Phase 2 signal suites remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bus payload → log.js | inference:result / signal:detected / response:* payloads (already PII-filtered upstream) cross into the logger |
| window.location → isActiveScreen | live pathname read gates whether any log line is written |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Denial of Service | src/log.js isActiveScreen / activeScreens gate | medium | mitigate | With the config.js array-type fix (04-01) in place, a malformed activeScreens hard-fails at init(); isActiveScreen treats absent/empty as permissive (fail-open for observability), and a valid list gates precisely on the live pathname. Tests assert both allow and block paths. |
| T-04-04 | Information Disclosure (PII) | src/log.js writeLog data | high | mitigate | writeLog only serializes fields already present on PII-filtered bus payloads (bbox/timestamp/intent/confidence/responseType); the module never reads element .value/.textContent/.innerHTML/localStorage/cookie. Firewall header comment restated; No-PII scope enforced by the fields the choke point copies. |
| T-04-06 | Repudiation | endSession double-count via unguarded lifecycle race | medium | mitigate | The module-level sessionEnded guard makes endSession fire exactly once regardless of whether flow:complete or pagehide arrives first, preventing a double gradient step (endSession is documented non-idempotent). Tested in both orderings. |
| T-04-05 | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase (04-RESEARCH.md Package Legitimacy Audit). |
</threat_model>

<verification>
- `npx vitest run tests/log.test.js tests/signal.test.js tests/signal-spa.test.js tests/config.test.js tests/inference.test.js tests/inference-endsession.test.js` all green.
- Single `[heed]` console.log call site confirmed in src/log.js only.
</verification>

<success_criteria>
- All 6 events log exactly one correctly-shaped, activeScreens-gated line.
- endSession fires exactly once per session across both lifecycle paths.
- flow:complete published once at completion; Phase 2 suites regression-free.
</success_criteria>

## Artifacts this phase produces (Plan 04-03)

- `src/log.js` — real `writeLog(config, sessionId, event, data)` (sole `[heed]` console.log site), `isActiveScreen(config)` (live pathname gate), `initLogging(config, sessionId)` (6 subscriptions + pagehide listener + sessionEnded guard + endSession call); internal `finishSession(outcome, event)`
- `src/signal.js` — `checkFlowComplete` now publishes `flow:complete` (new bus event) once at the false→true transition
- New bus events consumed/produced: `flow:complete` (published by signal.js), `response:fired` / `response:dismissed` (consumed by log.js; produced in Plan 04-04)

<output>
Create `.planning/phases/04-response-overlay-logging/04-03-SUMMARY.md` when done. Note that response:fired/response:dismissed handlers subscribe to events produced by Plan 04-04 (response.js) — until 04-04 lands, those two log lines are exercised only by the synthetic bus dispatch in tests/log.test.js.
</output>
