---
phase: 04-response-overlay-logging
plan: 02
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/response.js
  - src/log.js
  - tests/response.test.js
  - tests/log.test.js
autonomous: true
requirements: [RESP-01, RESP-02, RESP-03, LOG-01]
must_haves:
  truths:
    - "tests/response.test.js and tests/log.test.js both load without a module-resolution error (stub exports resolve every import)."
    - "Every RED test encodes an observable behavior for RESP-01/02/03, D-05, LOG-01, D-01/02/03, D-06/07 and currently fails against the stubs."
    - "clampToViewport() tests exercise BOTH the bbox-present path and the bbox-null (fallback) path — the null path is first-class, not an afterthought."
  artifacts:
    - "src/response.js stub exporting initResponse, clampToViewport, safeAreaInset"
    - "src/log.js stub exporting initLogging, isActiveScreen, writeLog"
    - "tests/response.test.js RED suite (RESP-01, RESP-02, RESP-03, D-05)"
    - "tests/log.test.js RED suite (LOG-01, session-lifecycle, activeScreens)"
  key_links:
    - "Stub exports are the contract surface the Wave-1/2 implementation plans fill in — the exported symbol names here lock the module API for index.js wiring"
    - "The bbox-null clampToViewport test is the guard that keeps discount_offer/social_proof (which NEVER carry a bbox) from silently regressing"
---

<objective>
Author the RED unit-test suites for this phase (`tests/response.test.js`, `tests/log.test.js`) and the minimal stub modules (`src/response.js`, `src/log.js`) that let those suites resolve their imports and fail on assertions rather than on a missing module (Phase 1-3 precedent: a missing named export fails the whole test file's module load).

Purpose: Lock the observable behavior contract for RESP-01/02/03, D-05, LOG-01, and the session-lifecycle/activeScreens decisions before implementation, per this project's established Wave-0-authors-RED-tests convention. The exported stub symbols also fix the module API (`initResponse`, `initLogging`, `clampToViewport`, `isActiveScreen`, etc.) that later plans and index.js wiring depend on.
Output: Two new test files (RED) and two new stub source files exporting the named functions the tests and index.js import.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-response-overlay-logging/04-RESEARCH.md
@.planning/phases/04-response-overlay-logging/04-UI-SPEC.md
@.planning/phases/04-response-overlay-logging/04-PATTERNS.md
@.planning/phases/04-response-overlay-logging/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub modules src/response.js and src/log.js so test imports resolve</name>
  <files>src/response.js, src/log.js</files>
  <read_first>
    - src/inference.js (initInference's one-time-registration + module-state reset shape at lines 222-278 — the exact idiom the stubs' signatures should mirror; also the inference:result publish shape at lines 266-277 that response.js/log.js consume)
    - src/signal.js (checkFlowComplete lines 240-247, maybeReattach live-pathname-read pattern — isActiveScreen must mirror the live read)
    - src/bus.js (publish/subscribe contract)
    - .planning/phases/04-response-overlay-logging/04-PATTERNS.md (src/response.js and src/log.js sections — the exact export names and firewall header-comment requirement)
    - .planning/phases/04-response-overlay-logging/04-RESEARCH.md (Patterns 1-4 — the function names to stub)
  </read_first>
  <action>
    Create src/response.js exporting plain named functions (no classes, no default export — established Phase 1-3 convention): `initResponse(config, sessionId)`, `clampToViewport(bbox, bubbleWidth, bubbleHeight)`, and `safeAreaInset(side)`. Each is a minimal stub: initResponse a no-op body, clampToViewport returning a placeholder object (e.g. `{ left: 0, top: 0 }`), safeAreaInset returning `0`. Add a header comment stating the No-PII firewall discipline (this module must never read `.value`/`.textContent`/`.innerHTML`/`localStorage`/`document.cookie` — it only consumes already-PII-filtered bus payloads), mirroring signal.js's buildPayload firewall comment. Create src/log.js exporting `initLogging(config, sessionId)`, `isActiveScreen(config)`, and `writeLog(config, sessionId, event, data)` as minimal stubs (initLogging no-op, isActiveScreen returning true, writeLog no-op). Add the same No-PII firewall header comment. Import `publish, subscribe` from './bus.js' in response.js and `subscribe` + `endSession` from './bus.js'/'./inference.js' in log.js only if referenced; a stub may leave imports out until the impl plan needs them. Do NOT implement real behavior here — these stubs exist only so the test files below load and fail on assertions, not on import resolution.
  </action>
  <verify>
    <automated>node -e "import('./src/response.js').then(m=>{if(!m.initResponse||!m.clampToViewport)process.exit(1)}); import('./src/log.js').then(m=>{if(!m.initLogging||!m.isActiveScreen||!m.writeLog)process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: src/response.js exports `initResponse`, `clampToViewport`, `safeAreaInset` via `export function`.
    - Source assertion: src/log.js exports `initLogging`, `isActiveScreen`, `writeLog` via `export function`.
    - Source assertion: both files contain a No-PII firewall header comment.
    - Both modules import-resolve with no error.
  </acceptance_criteria>
  <done>Both stub modules load and expose the named exports the test suites and index.js wiring will import.</done>
</task>

<task type="auto">
  <name>Task 2: Author tests/response.test.js RED suite (RESP-01, RESP-02, RESP-03, D-05)</name>
  <files>tests/response.test.js</files>
  <read_first>
    - tests/inference-endsession.test.js (RED-suite header convention + synthetic publish/subscribe bus-dispatch helper at lines 1-13 — copy this shape)
    - tests/signal-spa.test.js (Object.defineProperty stubbing of window props + history.pushState pattern — reuse for window.innerWidth/innerHeight and pathname)
    - .planning/phases/04-response-overlay-logging/04-UI-SPEC.md (Response Types copy table lines 96-101, positioning/clamping lines 105-112, postMessage shape lines 121-134, animation/auto-dismiss lines 114-119)
    - .planning/phases/04-response-overlay-logging/04-RESEARCH.md (Pitfall 2 bbox-null chain, Pitfall 5 no getBoundingClientRect in happy-dom, Test Map lines 632-643)
    - .planning/phases/04-response-overlay-logging/04-PATTERNS.md (tests/response.test.js section)
  </read_first>
  <action>
    Create tests/response.test.js with Vitest describe/it blocks named so `-t "RESP-01"`, `-t "RESP-02"`, `-t "RESP-03"`, and `-t "D-05"` each select their cases. RESP-01: after initResponse(config, sessionId) and publishing a synthetic `inference:result` with `fires:true`, assert a single container element exists in document.body with inline `pointer-events: none` and that the rendered response element carries `pointer-events: auto`; assert host DOM outside the overlay is untouched (a pre-inserted sentinel element is unchanged). RESP-02: call clampToViewport as a PURE function with an explicit `{x,y,width,height}` bbox plus explicit bubbleWidth/bubbleHeight and assert the returned left/top stay within safe-area-aware bounds; author a SEPARATE, equally-detailed case passing `null` as bbox and asserting the bottom-clamp fallback placement (this is the ONLY path discount_offer/social_proof ever take — Pitfall 2). Stub window.innerWidth/innerHeight via Object.defineProperty (never getBoundingClientRect — Pitfall 5). RESP-03: for each of the 4 intent classes (confusion→tooltip, price_doubt→discount_offer, trust_gap→social_proof, flow_friction→nudge_copy) assert the rendered bubble contains the exact UI-SPEC copy string; for discount_offer, spy on `window.parent.postMessage` and assert it is called with the exact payload shape `{type:'heed:discount_offer', sessionId, partnerId, intent:'price_doubt', timestamp}` and an explicit target origin equal to config.partnerOrigin (assert the second argument is NOT a wildcard target origin), and assert response.js performs no discount fulfillment logic. D-05: publish a second above-threshold `inference:result` while a bubble is showing and assert the first bubble is removed (only one bubble in the container) and a `response:dismissed` event carried `dismissReason` equal to the replaced-marker value before the new bubble rendered. Keep auto-dismiss-timer cases (using vi.useFakeTimers) in THIS file only, isolated from any file that drives initSignalCapture/MutationObserver (Pitfall 4). All cases currently fail against the stub — that is the intended RED state.
  </action>
  <verify>
    <automated>npx vitest run tests/response.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/response.test.js loads without a module-resolution error and RUNS (failures are assertion failures, not import errors).
    - The suite contains selectable cases for `RESP-01`, `RESP-02`, `RESP-03`, and `D-05`.
    - At least one RESP-02 case passes `null` as the bbox argument (bbox-null fallback coverage).
    - The RESP-03 discount_offer case asserts the postMessage target origin equals config.partnerOrigin and is not a wildcard.
  </acceptance_criteria>
  <done>A RED response suite exists, runs, and encodes RESP-01/02/03 + D-05 behavior including the bbox-null clamp path.</done>
</task>

<task type="auto">
  <name>Task 3: Author tests/log.test.js RED suite (LOG-01, session-lifecycle, activeScreens)</name>
  <files>tests/log.test.js</files>
  <read_first>
    - tests/inference-endsession.test.js (call-twice / assert-exactly-once style at lines 75-106 — reuse for the sessionEnded guard)
    - tests/signal-spa.test.js (history.pushState({},'', '/path') synthetic pathname technique)
    - .planning/phases/04-response-overlay-logging/04-UI-SPEC.md (Logging Contract table lines 152-166 — the 6 events, the {ts,sessionId,partnerId,event,data} shape, and the per-event data fields)
    - .planning/phases/04-response-overlay-logging/04-RESEARCH.md (Pattern 3 session-lifecycle wiring lines 330-376, Pitfall 3 no-real-routing, Test Map lines 639-641)
    - .planning/phases/04-response-overlay-logging/04-CONTEXT.md (D-01/D-02/D-03/D-05/D-06/D-07/D-08/D-09)
  </read_first>
  <action>
    Create tests/log.test.js with describe/it blocks named so `-t "LOG-01"`, `-t "session-lifecycle"`, and `-t "activeScreens"` each select their cases. LOG-01: spy on `console.log`; for each of the 6 event types (`signal_detected`, `inference_run`, `response_fired`, `response_dismissed`, `flow_complete`, `flow_abandoned`), drive the corresponding bus event or lifecycle path and assert EXACTLY ONE `console.log` call whose first arg is `'[heed]'` and whose second arg parses to `{ ts, sessionId, partnerId, event, data }` with `partnerId` equal to config.platformId (D-09) and `event` equal to the expected name; assert the per-event `data` carries the UI-SPEC fields (e.g. inference_run data has intent/confidence/fires; response_fired data has intent/responseType/targetSelector; response_dismissed data has responseType/dismissReason). session-lifecycle: publish `flow:complete` AND dispatch a `pagehide` event (assert both orderings — flow-first and pagehide-first) and assert the combined `endSession` call count is exactly 1 (spy/mock endSession) and exactly one of `flow_complete`/`flow_abandoned` is logged, never both (D-03 sessionEnded guard). activeScreens: use history.pushState to set window.location.pathname to a value inside config.activeScreens and assert writeLog emits; set it to a value OUTSIDE activeScreens and assert writeLog emits nothing (D-06); also assert an absent/empty activeScreens is permissive (emits). All cases currently fail against the stub — intended RED state.
  </action>
  <verify>
    <automated>npx vitest run tests/log.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/log.test.js loads without a module-resolution error and RUNS.
    - Selectable cases exist for `LOG-01`, `session-lifecycle`, and `activeScreens`.
    - The session-lifecycle case asserts endSession is called exactly once across a flow:complete + pagehide combination in BOTH orderings.
    - The activeScreens case drives pathname via history.pushState (not real navigation) per Pitfall 3.
  </acceptance_criteria>
  <done>A RED log suite exists, runs, and encodes LOG-01 + the sessionEnded guard + activeScreens gating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bus payload → response.js/log.js | Only PII-filtered payloads (inference:result, signal:detected) cross into the new modules; the test suite is where the firewall contract is first asserted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Information Disclosure | src/response.js, src/log.js No-PII firewall | high | mitigate | Stub header comments state the firewall rule at the point it could be violated; the RED suites never introduce a `.value`/`.textContent` read, establishing the PII-free baseline the implementation plans must preserve. |
| T-04-01 | Information Disclosure | tests/response.test.js discount_offer postMessage assertion | high | mitigate | The RESP-03 test asserts the postMessage target origin equals config.partnerOrigin and is not a wildcard — the test is the executable guard behind the no-wildcard-origin requirement. |
| T-04-05 | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase — no new dependencies (04-RESEARCH.md Package Legitimacy Audit). |
</threat_model>

<verification>
- `npx vitest run tests/response.test.js tests/log.test.js` RUNS (assertion failures, not import failures) — the RED baseline.
- Stub modules import-resolve with all named exports present.
</verification>

<success_criteria>
- Both RED suites run and fail on assertions only.
- Stub API surface (exports) matches what index.js wiring and the implementation plans will fill in.
- bbox-null clamp path and the exactly-once sessionEnded guard are both encoded as tests.
</success_criteria>

## Artifacts this phase produces (Plan 04-02)

- `src/response.js` (stub) — exports `initResponse(config, sessionId)`, `clampToViewport(bbox, bubbleWidth, bubbleHeight)`, `safeAreaInset(side)`
- `src/log.js` (stub) — exports `initLogging(config, sessionId)`, `isActiveScreen(config)`, `writeLog(config, sessionId, event, data)`
- `tests/response.test.js` — RED suite covering RESP-01, RESP-02, RESP-03, D-05
- `tests/log.test.js` — RED suite covering LOG-01, session-lifecycle (D-01/02/03), activeScreens (D-06/07)

<output>
Create `.planning/phases/04-response-overlay-logging/04-02-SUMMARY.md` when done. Record the exact exported symbol names (module API surface) so the implementation plans and index.js wiring bind to them exactly.
</output>
