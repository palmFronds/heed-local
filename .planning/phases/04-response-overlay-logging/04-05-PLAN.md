---
phase: 04-response-overlay-logging
plan: 05
type: execute
wave: 3
depends_on: ["04-03", "04-04"]
files_modified:
  - src/index.js
  - tests/e2e/harness.spec.js
  - test-harness/index.html
autonomous: true
requirements: [RESP-01, RESP-02, RESP-03, LOG-01]
must_haves:
  truths:
    - "init() generates one sessionId per page load via crypto.randomUUID() and passes it to initLogging and initResponse."
    - "The built dist/sdk.js wires response + logging so a triggered signal renders a bubble and logs the pipeline in a real mobile-emulated browser."
    - "The overlay renders above the harness UI without blocking taps/scroll on the host page underneath."
    - "discount_offer's postMessage is observable in the harness parent context for manual verification."
  artifacts:
    - "src/index.js wiring initLogging(config, sessionId) + initResponse(config, sessionId) with a crypto.randomUUID sessionId"
    - "tests/e2e/harness.spec.js response-rendering + postMessage-capture assertions"
    - "test-harness/index.html message listener + any debug wiring needed to trigger inference:result for manual verify"
  key_links:
    - "index.js is the single place sessionId is generated and threaded into both new modules (D-08)"
    - "the e2e build gate proves the two new modules bundle into dist/sdk.js via the existing import graph"
---

<objective>
Wire the two new modules into `src/index.js`'s `init()` — generate one `sessionId` per page load via `crypto.randomUUID()` (D-08) and pass it to `initLogging(config, sessionId)` and `initResponse(config, sessionId)` — then extend the existing Playwright E2E harness with response-rendering and postMessage-capture assertions, rebuild `dist/sdk.js`, and add a host-page message listener to the test harness for manual discount_offer verification.

Purpose: index.js is the thin orchestrator that owns sessionId generation and side-effecting module wiring. Until this lands, response.js/log.js are built and unit-tested but not actually initialized by the harness. The E2E layer proves the two new modules bundle and drive real DOM/console behavior a happy-dom unit test cannot.
Output: A wired index.js, extended E2E spec, rebuilt bundle, and a manual-verify hook in the harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-response-overlay-logging/04-RESEARCH.md
@.planning/phases/04-response-overlay-logging/04-VALIDATION.md
@src/index.js
@src/response.js
@src/log.js
@tests/e2e/harness.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire sessionId generation and initLogging/initResponse into src/index.js init()</name>
  <files>src/index.js</files>
  <read_first>
    - src/index.js (the whole file — init() lines 20-34 is the exact "validate first, then wire side-effecting modules" shape to extend; imports at lines 4-9)
    - src/log.js (initLogging export signature)
    - src/response.js (initResponse export signature)
    - .planning/phases/04-response-overlay-logging/04-PATTERNS.md (src/index.js MODIFY section — the exact extension shape and import additions)
    - .planning/phases/04-response-overlay-logging/04-RESEARCH.md (D-08 — sessionId once per page load inside init(); crypto.randomUUID verified over file:// lines 527-538)
    - tests/index.test.js (existing init() assertions — must stay green)
  </read_first>
  <action>
    In src/index.js add `import { initLogging } from './log.js';` and `import { initResponse } from './response.js';` alongside the existing named imports (lines 4-9), matching the no-default-export convention. Inside init(rawConfig), after `const config = validateConfig(rawConfig, schema);`, add `const sessionId = crypto.randomUUID();` (D-08 — once per page load; native API, no fallback needed for the file:///localhost test contexts per RESEARCH). Keep the existing initSignalCapture(config) and initInference(config) calls, then add `initLogging(config, sessionId);` and `initResponse(config, sessionId);` in that order (log.js owns session-lifecycle wiring, so init it before response so its subscriptions are registered when the first inference:result could arrive). Do NOT change the return shape — keep returning { config, publish, subscribe }; sessionId stays internal module state, not a returned value. initDemo() needs no change (it delegates to init()).
  </action>
  <verify>
    <automated>npx vitest run tests/index.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: src/index.js init() calls `crypto.randomUUID()` exactly once and passes the same sessionId to both initLogging and initResponse.
    - Source assertion: init() imports initLogging from './log.js' and initResponse from './response.js'.
    - Source assertion: init()'s return shape remains `{ config, publish, subscribe }` (no new key).
    - Regression: tests/index.test.js stays GREEN.
  </acceptance_criteria>
  <done>init() generates one sessionId and wires both new modules; existing init tests stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the E2E harness with response-rendering + postMessage-capture assertions, add a manual-verify message listener, and rebuild the bundle</name>
  <files>tests/e2e/harness.spec.js, test-harness/index.html</files>
  <read_first>
    - tests/e2e/harness.spec.js (the whole file — existing signal-receipt tests + the file:// HARNESS_URL + page.on/console conventions to extend)
    - test-harness/index.html (debug-panel buttons lines ~139-152, the #log panel, the inline script that dispatches synthetic-but-real DOM events lines ~176-221 — the D-08 "real listener, synthetic trigger" precedent)
    - .planning/phases/04-response-overlay-logging/04-RESEARCH.md (Code Examples lines 540-556 — synthetic pagehide/postMessage in E2E; Open Question 1 — prefer synthetic dispatch over real navigation)
    - .planning/phases/04-response-overlay-logging/04-VALIDATION.md (Manual-Only Verifications lines 69-72 — the message-listener manual check; Wave 0 Requirements line 59, 653)
  </read_first>
  <action>
    Extend tests/e2e/harness.spec.js (do NOT create a new E2E file) with response-focused tests selectable by `-g "response"`: after building, load the harness, trigger a signal that produces an above-threshold inference:result, and assert (a) an overlay element with the data-heed-overlay marker exists with pointer-events: none, (b) a rendered response bubble appears with pointer-events: auto and the expected UI-SPEC copy for its intent, and (c) the host page underneath remains interactive (e.g. a data-heed element is still clickable / receives the tap — proving the container's pointer-events: none lets taps through). For discount_offer, capture the postMessage in the harness by asserting on the message the harness's own listener logs (see harness change below) or via page.evaluate capturing a window 'message' event, and assert the payload type is heed:discount_offer with an explicit (non-wildcard) origin. Use synthetic dispatch (page.evaluate driving the debug panel / bus) rather than real navigation for any pagehide/flow_abandoned coverage (RESEARCH Open Question 1). In test-harness/index.html add a `window.addEventListener('message', ...)` listener in the parent context that appends received discount_offer payloads into the #log panel (or a dedicated marker) so the discount_offer postMessage is visible for manual verification (VALIDATION Manual-Only table) and assertable by the E2E test. Then rebuild: run `npm run build` so dist/sdk.js reflects the new src/index.js wiring and the two new modules (the E2E suite requires a fresh build per the existing harness.spec.js convention).
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/e2e/harness.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds and dist/sdk.js includes the response + logging modules (bundle grows / contains a data-heed-overlay marker — `grep -c "data-heed-overlay" dist/sdk.js` is at least 1).
    - `npx playwright test tests/e2e/harness.spec.js -g "response"` is GREEN.
    - Existing signal-receipt E2E tests stay GREEN.
    - test-harness/index.html contains a `window.addEventListener('message'` listener that surfaces discount_offer payloads.
  </acceptance_criteria>
  <done>The bundle builds with both new modules; E2E proves the overlay renders above the host without blocking interaction and the discount_offer postMessage is observable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| init() → module wiring | sessionId (crypto.randomUUID, a correlation id, not an auth token) is generated once and threaded into the logging/response modules |
| harness parent ← postMessage | the harness message listener receives discount_offer payloads across the frame boundary for verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Spoofing / Session Management | index.js sessionId | low | accept | sessionId is a crypto.randomUUID correlation identifier only — it grants no privilege, carries no server-side state, and is not derived from user data (no session-fixation/hijacking surface). Native RNG per ASVS V6; never hand-rolled. |
| T-04-01 | Information Disclosure | E2E postMessage-capture assertion | high | mitigate | The E2E test asserts the discount_offer postMessage origin is explicit (non-wildcard), giving a real-browser guard on top of the unit-level assertion (T-04-01). |
| T-04-05 | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase (04-RESEARCH.md Package Legitimacy Audit). |
</threat_model>

<verification>
- `npm run build` succeeds; `npx playwright test tests/e2e/harness.spec.js` and `npm test` (full Vitest) both green.
- sessionId generated once and threaded into both modules.
</verification>

<success_criteria>
- init() wires sessionId + both modules; return shape unchanged.
- E2E proves overlay-above-host-without-blocking and observable discount_offer postMessage.
- Full unit + E2E suites green after a fresh build.
</success_criteria>

## Artifacts this phase produces (Plan 04-05)

- `src/index.js` — `init()` now generates `sessionId` via `crypto.randomUUID()` (D-08) and calls `initLogging(config, sessionId)` + `initResponse(config, sessionId)`; new imports of both modules
- `tests/e2e/harness.spec.js` — new response-rendering + postMessage-capture tests (selectable via `-g "response"`)
- `test-harness/index.html` — parent-context `window.addEventListener('message', ...)` surfacing discount_offer payloads for manual verify
- `dist/sdk.js` — rebuilt to include response.js + log.js via the existing esbuild import graph

<output>
Create `.planning/phases/04-response-overlay-logging/04-05-SUMMARY.md` when done. Note that true end-of-page-lifecycle pagehide firing is covered only by synthetic dispatch + manual verification (RESEARCH Open Question 1), deferred to Phase 6's real-device sequence.
</output>
