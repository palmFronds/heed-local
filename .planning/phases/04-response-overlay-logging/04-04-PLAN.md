---
phase: 04-response-overlay-logging
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/response.js
  - .planning/phases/04-response-overlay-logging/04-UI-SPEC.md
autonomous: true
requirements: [RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "One fixed full-viewport overlay container is injected once at init with pointer-events: none; every rendered response element carries pointer-events: auto."
    - "clampToViewport keeps a bubble within safe-area-aware bounds for BOTH a real bbox and a null bbox (bottom-clamp fallback)."
    - "All 4 response types render their exact UI-SPEC copy for their mapped intent class."
    - "discount_offer fires window.parent.postMessage with the locked payload shape and an explicit config.partnerOrigin target (never a wildcard) and performs no fulfillment logic."
    - "A new above-threshold inference:result while a bubble is showing dismisses the old bubble (dismissReason replaced) before rendering the new one — only one bubble ever visible."
  artifacts:
    - "src/response.js real implementation: overlay container, clampToViewport, safeAreaInset, 4 response renderers, postMessage, single-bubble concurrency"
    - "04-UI-SPEC.md dismissReason enum updated with the 4th value (D-05)"
  key_links:
    - "response.js publishes response:fired/response:dismissed onto the bus, which log.js (04-03) consumes — this indirection preserves 'only log.js calls console.log'"
    - "the bbox-null fallback is the ONLY clamp path for discount_offer/social_proof — a first-class path, not an edge case"
    - "response.js imports isActiveScreen from log.js (04-03) — the shared single gate (A5) that must not drift from log.js's gate"
---

<objective>
Implement `src/response.js`'s real behavior: inject the single fixed full-viewport overlay container (RESP-01), position bubbles with a safe-area-aware `clampToViewport()` whose null-bbox fallback is a first-class path (RESP-02), render all 4 response types with their exact UI-SPEC copy and fire `discount_offer`'s explicit-origin `postMessage` (RESP-03), and enforce single-bubble-at-a-time concurrency with the new `"replaced"` dismissReason (D-05). Also update `04-UI-SPEC.md`'s dismissReason enum to add the 4th value (D-05).

Purpose: This is the visible surface of the phase. response.js consumes `inference:result`, renders a bubble only when `fires && isActiveScreen(config)` (D-06), and publishes `response:fired`/`response:dismissed` onto the bus so log.js (never response.js) writes the log lines.
Output: A fully implemented response.js turning the RED response suite GREEN, plus the UI-SPEC enum doc update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-response-overlay-logging/04-RESEARCH.md
@.planning/phases/04-response-overlay-logging/04-UI-SPEC.md
@.planning/phases/04-response-overlay-logging/04-PATTERNS.md
@src/inference.js
@src/bus.js
@src/log.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement src/response.js — overlay, clampToViewport, 4 response types, postMessage, single-bubble concurrency</name>
  <files>src/response.js</files>
  <read_first>
    - src/response.js (the Plan 04-02 stub — replace bodies, keep export names initResponse/clampToViewport/safeAreaInset)
    - tests/response.test.js (the RED suite this task turns GREEN — read exact copy strings, postMessage shape, and D-05 assertions)
    - src/log.js (isActiveScreen export — response.js imports and reuses it, the shared A5 gate; do not duplicate)
    - src/inference.js (inference:result publish shape lines 266-277 — response.js reads fires/intent/bbox/targetSelector)
    - src/bus.js (publish/subscribe contract — response.js publishes response:fired/response:dismissed)
    - .planning/phases/04-response-overlay-logging/04-RESEARCH.md (Pattern 1 overlay lines 264-282, Pattern 2 clampToViewport + safeAreaInset lines 284-327, Pattern 4 postMessage lines 378-396)
    - .planning/phases/04-response-overlay-logging/04-UI-SPEC.md (Response Types copy table lines 96-101, positioning lines 105-112, postMessage shape lines 121-134, animation/auto-dismiss/dismiss-control lines 103-119, styling mechanism lines 28-35)
    - .planning/phases/04-response-overlay-logging/04-PATTERNS.md (src/response.js section — initResponse one-time-registration guard, single dismiss-payload choke point)
  </read_first>
  <behavior>
    - initResponse(config, sessionId): injects exactly one overlay container into document.body with inline pointer-events: none; guards the inference:result subscription with an initialized boolean; resets current-bubble/timer state every call; the subscription handler renders only when payload.fires === true AND isActiveScreen(config) is true.
    - Every rendered response element gets inline pointer-events: auto.
    - clampToViewport(bbox, bubbleWidth, bubbleHeight): with a real bbox returns a below/above-anchor placement clamped to safe-area bounds; with null bbox returns the bottom-clamp fallback (the only path for discount_offer/social_proof); never returns NaN (safeAreaInset falls back to 0).
    - Intent→type→copy: confusion→tooltip, price_doubt→discount_offer, trust_gap→social_proof, flow_friction→nudge_copy, each with the exact UI-SPEC string; each bubble has a 44×44px-hit-target dismiss control (aria-label Dismiss).
    - discount_offer: on render, calls window.parent.postMessage with the locked payload and an explicit config.partnerOrigin target (never wildcard); contains no discount fulfillment logic. discount_offer does not auto-dismiss; the other 3 auto-dismiss after 6000ms.
    - D-05: a second above-threshold inference:result while a bubble shows dismisses the existing bubble (publishing response:dismissed with the replaced dismissReason) then renders the new one — only one bubble in the container at any time.
    - Every dismiss path (manual ×, CTA tap, 6000ms timeout, replaced) routes through ONE dismiss choke point that publishes response:dismissed with the correct dismissReason; render publishes response:fired.
  </behavior>
  <action>
    Replace the src/response.js stub with the real implementation (keep exported names). Import publish, subscribe from './bus.js' and isActiveScreen from './log.js' (shared gate — A5; do not re-implement the pathname check). Follow initInference's one-time-registration + reset-on-reinit idiom: reset module-level current-bubble and timer references every initResponse call, but guard the subscribe('inference:result', ...) registration and the overlay-container injection behind an initialized boolean so repeat init() never stacks a second subscription or second container. Build the overlay container per RESEARCH Pattern 1 (fixed, top/left 0, 100% w/h, inline pointer-events: none, high z-index, overflow hidden, box-sizing border-box) with a data-heed-overlay marker attribute (NOT one of the 7 locked selectors). Set ALL presentation via inline element.style (never a <style> tag + classes — host CSS cascade isolation, UI-SPEC styling mechanism); every rendered response element gets inline pointer-events: auto. Implement clampToViewport and safeAreaInset per RESEARCH Pattern 2 verbatim intent: bbox-present → below-anchor default, flip above if it overflows the safe-bottom, final clamp to safe bounds; bbox-null → bottom-clamp fallback; safeAreaInset probes env(safe-area-inset-<side>, 0px) via a computed-style probe element and returns 0 rather than NaN when unavailable. Map each of the 4 intent classes to its response type and exact UI-SPEC copy string; render a dismiss control with a 44×44px hit target and aria-label Dismiss. For discount_offer, call window.parent.postMessage with the payload { type: 'heed:discount_offer', sessionId, partnerId: config.platformId, intent, timestamp: Date.now() } and config.partnerOrigin as the explicit target origin — never a wildcard target origin, and add no code that grants/applies/fulfills a discount. Auto-dismiss tooltip/nudge_copy/social_proof after 6000ms via a timer; discount_offer persists until CTA or × tap. Implement one dismiss choke point (e.g. dismissCurrent(dismissReason)) called from the × handler ('manual'), CTA handler ('cta'), the 6000ms timeout ('timeout'), and the D-05 replacement path (the replaced-marker value) — it removes the bubble, clears its timer, and publishes response:dismissed with { responseType, dismissReason }. On render publish response:fired with { intent, responseType, targetSelector }. Keep the No-PII firewall header comment — never read any element's value/textContent/innerHTML. Respect prefers-reduced-motion (opacity-only) per UI-SPEC animation.
  </action>
  <verify>
    <automated>npx vitest run tests/response.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/response.test.js -t "RESP-01"`, `-t "RESP-02"`, `-t "RESP-03"`, and `-t "D-05"` are all GREEN.
    - Source assertion: src/response.js sets `pointer-events: none` on the container and `pointer-events: auto` on rendered elements (inline styles).
    - Source assertion: the discount_offer postMessage passes config.partnerOrigin as the target origin and no wildcard target-origin literal appears in src/response.js.
    - Source assertion: response.js imports isActiveScreen from './log.js' (does not re-implement the pathname gate).
    - Source assertion: response.js contains no `.value`/`.textContent`/`.innerHTML`/`localStorage`/`document.cookie` read.
    - Behavior: rendering a second above-threshold result leaves exactly one bubble in the container and emits a response:dismissed with the replaced dismissReason.
  </acceptance_criteria>
  <done>All 4 response types render correctly, clamp on both bbox paths, postMessage uses an explicit origin, single-bubble concurrency works; the RED response suite is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Add the 4th dismissReason value to 04-UI-SPEC.md's enum (D-05)</name>
  <files>.planning/phases/04-response-overlay-logging/04-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/04-response-overlay-logging/04-UI-SPEC.md (Logging Contract table, response_dismissed row line 161 — the current `"manual" | "cta" | "timeout"` enum)
    - .planning/phases/04-response-overlay-logging/04-CONTEXT.md (D-05 — the mandate to add the 4th value and flag it back to UI-SPEC)
  </read_first>
  <action>
    In 04-UI-SPEC.md's Logging Contract table, extend the response_dismissed row's dismissReason enum from three values to four by adding the replacement-case value (the one D-05 introduces for a bubble dismissed because a newer above-threshold response took its place). Add a short note that the 4th value was added per CONTEXT.md D-05 (single-bubble concurrency). This is a documentation-consistency update so the locked UI-SPEC enum matches the implemented response:dismissed payload shape — do not change any other locked UI-SPEC value.
  </action>
  <verify>
    <automated>grep -c "replaced" .planning/phases/04-response-overlay-logging/04-UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: 04-UI-SPEC.md's response_dismissed dismissReason enum now lists four values including the replacement-case value.
    - Source assertion: a note references CONTEXT.md D-05 as the reason for the addition.
    - No other locked UI-SPEC value changed (diff limited to the response_dismissed row + note).
  </acceptance_criteria>
  <done>The UI-SPEC dismissReason enum documents all four values, matching response.js's implemented payload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| response.js → window.parent (postMessage) | discount_offer signal crosses the frame boundary to the host page — the primary information-disclosure surface |
| host-page CSS → overlay DOM | the host's own stylesheet can cascade into overlay nodes (no Shadow DOM in v1) |
| bus payload → response.js | only PII-filtered inference:result payloads cross in |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | response.js discount_offer window.parent.postMessage | high | mitigate | postMessage always passes config.partnerOrigin as an explicit target origin (never a wildcard); partnerOrigin is schema-required (04-01) so it can never be undefined. Test asserts the target-origin argument equals config.partnerOrigin and no wildcard literal exists in source. |
| T-04-02 | Tampering (rendered UI) | response.js overlay/bubble styling | medium | mitigate | All presentation set via inline element.style with explicit values (no <style>+class), so a host CSS cascade cannot silently corrupt appearance or hide the dismiss control; !important only where a specific collision is anticipated (UI-SPEC styling mechanism). |
| T-04-04 | Information Disclosure (PII) | response.js DOM handling | high | mitigate | response.js never reads element .value/.textContent/.innerHTML/localStorage/cookie; it only consumes PII-filtered bus payloads and writes fixed copy strings. Firewall header comment restated; asserted by source grep. |
| T-04-05 | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase (04-RESEARCH.md Package Legitimacy Audit). |
</threat_model>

<verification>
- `npx vitest run tests/response.test.js tests/log.test.js` fully green.
- Source grep confirms no wildcard postMessage target origin and no PII reads in response.js.
- UI-SPEC dismissReason enum lists four values.
</verification>

<success_criteria>
- RESP-01/02/03 + D-05 suite green.
- discount_offer postMessage uses explicit origin, fulfills nothing.
- Single-bubble concurrency with the replaced dismissReason works.
- UI-SPEC enum documentation matches implementation.
</success_criteria>

## Artifacts this phase produces (Plan 04-04)

- `src/response.js` — real `initResponse(config, sessionId)` (overlay injection + inference:result subscription + single-bubble concurrency), `clampToViewport(bbox, bubbleWidth, bubbleHeight)` (safe-area-aware, null-bbox fallback), `safeAreaInset(side)` (env() computed-style probe, 0 fallback); internal renderers for tooltip/nudge_copy/discount_offer/social_proof; internal dismiss choke point
- New bus events produced by response.js: `response:fired` ({ intent, responseType, targetSelector }), `response:dismissed` ({ responseType, dismissReason })
- postMessage payload type: `heed:discount_offer` ({ type, sessionId, partnerId, intent, timestamp })
- `04-UI-SPEC.md` — dismissReason enum extended to 4 values (D-05)

<output>
Create `.planning/phases/04-response-overlay-logging/04-04-SUMMARY.md` when done. Record the response:fired/response:dismissed payload field sets and the exact 4th dismissReason value for downstream (index.js wiring + e2e) reference.
</output>
