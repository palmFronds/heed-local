---
phase: 04-response-overlay-logging
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/config.js
  - config/schema.json
  - config/demo-platform.json
  - tests/config.test.js
autonomous: true
requirements: [RESP-03, LOG-01]
must_haves:
  truths:
    - "A config declaring activeScreens as an array validates cleanly instead of hard-failing on typeof === 'object'."
    - "A config declaring activeScreens as a non-array (string/number/object) hard-fails at init() with a thrown Error (CFG-02 discipline)."
    - "demo-platform.json carries a concrete activeScreens list and a partnerOrigin, both accepted by validateConfig."
    - "partnerOrigin is a required, validated string so RESP-03's postMessage can never fall back to a wildcard origin."
  artifacts:
    - "src/config.js walk() with Array.isArray array-type support"
    - "config/schema.json declaring activeScreens (array), partnerOrigin (required string)"
    - "config/demo-platform.json with activeScreens value ([ASSUMED]) and partnerOrigin"
    - "tests/config.test.js array-type regression cases"
  key_links:
    - "walk() array branch is what unblocks every downstream activeScreens gate in log.js/response.js"
    - "partnerOrigin in the required[] array is the schema-level enforcement behind T-04-01's no-wildcard-origin mitigation"
---

<objective>
Fix the verified `src/config.js` array-type validation bug (Pitfall 1, 04-RESEARCH.md) and extend the config schema + demo config with the two new fields this phase needs: `activeScreens` (array, gates logging/response layers) and `partnerOrigin` (required string, the explicit postMessage target origin for RESP-03).

Purpose: The array-type bug hard-fails EVERY valid config that declares `activeScreens` as an array, because `typeof [] === 'object'` in JS and `walk()` has no array special-case. This is a blocking prerequisite for the entire phase — log.js and response.js cannot read `config.activeScreens` until the schema accepts it, and the schema cannot declare `"type": "array"` until `walk()` understands arrays.
Output: A patched `config.js`, an extended `schema.json` + `demo-platform.json`, and array-type regression cases added to the existing `tests/config.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-response-overlay-logging/04-RESEARCH.md
@.planning/phases/04-response-overlay-logging/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add array-type support to config.js walk() with a failing regression test first</name>
  <files>tests/config.test.js, src/config.js</files>
  <read_first>
    - src/config.js (the whole file — walk() is lines 15-37; the bug is the type-mismatch clause at lines 16-23 that only special-cases 'object', never 'array')
    - tests/config.test.js (existing Phase 1 suite — copy its describe/it structure and its validateConfig invocation convention)
    - .planning/phases/04-response-overlay-logging/04-RESEARCH.md (Pitfall 1 — the verified `node -e` reproduction and the exact fix guidance)
    - .planning/phases/04-response-overlay-logging/04-PATTERNS.md (src/config.js section — "add an Array.isArray(value) branch mirroring the existing object special-case structurally")
  </read_first>
  <behavior>
    - RED first: a case validating `{ activeScreens: ['/swap','/confirm'] }` against a schema node `{ type: 'array' }` currently THROWS "expected type array, got object" — assert it should NOT throw.
    - After fix: an array value passes when schema type is 'array'.
    - After fix: a non-array value (e.g. the string '/swap', or a number, or a plain object) against `{ type: 'array' }` still THROWS with an `expected type "array"` message (hard-fail preserved, CFG-02).
    - Existing object/string/number/enum/required cases in config.test.js still pass unchanged (no regression).
  </behavior>
  <action>
    In tests/config.test.js add a new describe block titled "array type validation" with (a) a case asserting validateConfig does not throw for an array value under a `{ type: 'array' }` schema node, and (b) a case asserting it DOES throw for each of a string, a number, and a plain object under the same node. Run the suite to confirm case (a) is RED against the current config.js. Then patch src/config.js walk(): the base-type-mismatch guard at the top of walk() must treat `schemaNode.type === 'array'` specially exactly the way `'object'` is already special-cased — when the declared type is 'array', validity is `Array.isArray(value)`, not `typeof value === 'array'` (which is never true). Add an `Array.isArray(value)` branch to the existing negated condition so an array value with `type: 'array'` no longer pushes a type error, and a non-array value still does. Keep the existing `errors.push(...)` message format identical (`${path}: expected type "${schemaNode.type}", got "${typeof value}"`). Do NOT add `additionalProperties` enforcement — that is a documented out-of-scope pre-existing gap (04-RESEARCH.md Open Question 2). Preserve the throw-never-soft-fail discipline (CFG-02).
  </action>
  <verify>
    <automated>npx vitest run tests/config.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/config.test.js -t "array"` is GREEN.
    - Full `tests/config.test.js` suite is GREEN (no regression in the Phase 1 cases).
    - Source assertion: src/config.js walk() contains an `Array.isArray` check; no bare `typeof value === 'array'` comparison exists anywhere.
  </acceptance_criteria>
  <done>An array value validates under `type: 'array'`; a non-array hard-fails; all prior config.test.js cases stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend schema.json and demo-platform.json with activeScreens and partnerOrigin</name>
  <files>config/schema.json, config/demo-platform.json</files>
  <read_first>
    - config/schema.json (the whole file — top-level `properties` and `required` at lines 3-5; add siblings to platformId/selectors)
    - config/demo-platform.json (the whole file — flat key convention)
    - .planning/phases/04-response-overlay-logging/04-PATTERNS.md (config/schema.json and config/demo-platform.json sections — exact fields and the required[] guidance)
    - .planning/phases/04-response-overlay-logging/04-RESEARCH.md (Assumptions A2 and A3 — activeScreens is [ASSUMED], partnerOrigin = http://localhost:3000 from repo0's runtime map)
  </read_first>
  <action>
    In config/schema.json add two top-level properties as siblings of platformId: `activeScreens` with `{ "type": "array" }`, and `partnerOrigin` with `{ "type": "string" }`. Add `partnerOrigin` to the top-level `required` array (it must never be undefined — RESP-03's postMessage target origin must be an explicit validated string per T-04-01, not left optional where it could force a wildcard fallback). Do NOT add `activeScreens` to required — an absent/empty activeScreens is a permissive "no gate" default (isActiveScreen returns true), which is the intended fallback. Optionally add `responses` as `{ "type": "object" }` (D-04, Claude's discretion — include it as an unused optional placeholder for schema-validation ergonomics, but do NOT add it to required and do NOT set it in demo-platform.json). In config/demo-platform.json add `"activeScreens": ["/swap", "/confirm", "/success"]` (tag it [ASSUMED] in a sibling comment is not possible in JSON — instead note the [ASSUMED] status in this plan's SUMMARY and rely on the SUMMARY/STATE record; these are placeholder routes pending Branch 1's real Next.js routes, everything except the entry screen "/") and `"partnerOrigin": "http://localhost:3000"` (repo0_overview.txt runtime map: Repo 1 runs on localhost:3000). Do NOT set a `responses` value in demo-platform.json (D-04).
  </action>
  <verify>
    <automated>npx vitest run tests/config.test.js tests/index.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `init(demoConfig)` / `initDemo()` does NOT throw a config error after the schema + demo changes land (demo-platform.json's new array value is accepted by the patched walk()).
    - Source assertion: config/schema.json top-level `required` array includes `"partnerOrigin"`.
    - Source assertion: config/demo-platform.json contains an `activeScreens` array and a `partnerOrigin` string equal to `http://localhost:3000`.
    - Source assertion: `activeScreens` is NOT in schema.json's required[] (permissive default preserved).
  </acceptance_criteria>
  <done>demo-platform.json validates cleanly with the two new fields; partnerOrigin is schema-required; activeScreens is optional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config author → validateConfig | An untrusted/malformed config object crosses into the SDK at init() time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Denial of Service | src/config.js walk() `activeScreens` validation | medium | mitigate | Fix the array-type bug so a malformed (non-array) `activeScreens` hard-fails at init() rather than silently passing the `typeof === 'object'` check and later gating all logging closed. Regression test asserts both the pass and hard-fail paths. |
| T-04-01 | Information Disclosure | config/schema.json `partnerOrigin` | high | mitigate | Make `partnerOrigin` a required, validated string so RESP-03's discount_offer postMessage always has an explicit target origin and can never fall back to a wildcard that any embedding frame could receive. Schema `required[]` membership is the enforcement point. |
| T-04-05 | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase — no new dependencies (04-RESEARCH.md Package Legitimacy Audit: nothing to audit). |
</threat_model>

<verification>
- `npx vitest run tests/config.test.js tests/index.test.js` is fully green.
- No new npm dependency added (verify package.json unchanged).
</verification>

<success_criteria>
- Array-type schema fields validate correctly (accept arrays, hard-fail non-arrays).
- demo-platform.json carries activeScreens and partnerOrigin and validates cleanly.
- partnerOrigin is schema-required.
</success_criteria>

## Artifacts this phase produces (Plan 04-01)

- `src/config.js` — `walk()` gains `Array.isArray` array-type handling
- `config/schema.json` — new top-level properties: `activeScreens` (array), `partnerOrigin` (required string), optional `responses` (object) placeholder
- `config/demo-platform.json` — new values: `activeScreens` (array, [ASSUMED] routes), `partnerOrigin` (`http://localhost:3000`)
- `tests/config.test.js` — new "array type validation" describe block

<output>
Create `.planning/phases/04-response-overlay-logging/04-01-SUMMARY.md` when done. Record the [ASSUMED] status of demo-platform.json's activeScreens value (placeholder pending Branch 1's real routes, revisit in Phase 6).
</output>
