---
phase: 05-weight-push-learning-loop
plan: 05
type: execute
wave: 2
depends_on: ["05-02", "05-03"]
files_modified:
  - admin/soak-test-weights.mjs
autonomous: true
requirements: [WEIGHT-01]

must_haves:
  truths:
    - "admin/soak-test-weights.mjs runs 10-20 synthetic sessions back-to-back, POSTing each session's endSession() weight update through the REAL local receiver (05-02), and prints softmax margins for the 4 canonical test signals before and after the run (SC3)."
    - "The script gates on SC3: after the run, no canonical signal's softmax collapses toward uniform (~0.25 each, near-zero margin) or saturates (~1.0 top prob) — process.exit(1) on any gate failure, exit(0) on pass, mirroring admin/print-softmax-margins.mjs."
    - "The script reads the final persisted weights back through the receiver's GET /weights endpoint and runs forwardPass against them, proving a cold-start restart would load learned (non-cold-start) weights — the deterministic SC2 verification gate."
    - "Zero new npm dependencies: Node global fetch + existing src/inference.js / src/bus.js imports only (D-04)."
  artifacts:
    - admin/soak-test-weights.mjs
  key_links:
    - "The soak loop exercises the full persistence round-trip: endSession() (05-03) → POST → receiver atomic write (05-02) → GET readback → initInference(config with those weights) → next session, so SC2 and SC3 are proven against the same code path the harness bootstrap (05-04) uses at runtime."
    - "Each session seeds lastInference by publishing a synthetic signal:detected on the same bus src/inference.js subscribes to (mirroring tests/inference-endsession.test.js's seeding), so endSession() has a prediction to reinforce."
    - "This is a manual/CI script gate (process.exit 0/1), NOT a Vitest assertion — matches 05-VALIDATION.md's Manual-Only Verifications posture and admin/print-softmax-margins.mjs's precedent."
---

<objective>
Author admin/soak-test-weights.mjs (D-08): a standalone Node script that drives 10-20 synthetic sessions through the real learning loop — calling endSession() directly, POSTing each update through the running receiver, and reading the persisted weights back through the receiver's GET endpoint — then prints and gates softmax margins for the canonical test signals before vs after. This is SC3's verification and, via the GET-readback, the deterministic gate for SC2.

Purpose: Prove the weight-push loop is stable over many updates (SC3) and that persisted learned weights are what a cold-start restart would load (SC2), without a browser.
Output: admin/soak-test-weights.mjs — a self-gating dev script run as `node admin/soak-test-weights.mjs` (npm run soak-test, wired in 05-02) against a running `npm run receiver`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-weight-push-learning-loop/05-RESEARCH.md
@.planning/phases/05-weight-push-learning-loop/05-PATTERNS.md
@admin/print-softmax-margins.mjs
@admin/weights.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Soak-test core — synthetic sessions, POST through real receiver, before/after SC3 margin gate</name>
  <files>admin/soak-test-weights.mjs</files>
  <read_first>
    - admin/print-softmax-margins.mjs (FULL file — the exact analog: header comment framing, CLASSES/SIGNAL_ORDER constants, argmax()/margin()/printVector() helpers, and the allPass + process.exit(0/1) gate/exit pattern to reuse verbatim)
    - admin/weights.js (the cold-start {W1,b1,W2,b2} export — the "before" baseline weights)
    - src/inference.js (exports to import: forwardPass, initInference, endSession; the module-scoped CLASSES/SIGNAL_ORDER order that MUST match admin/generate-weights.mjs's canonicalData order)
    - tests/inference-endsession.test.js (the pattern for seeding lastInference — initInference(config) then publish a synthetic signal:detected on the bus so endSession has a prediction to reinforce)
    - .planning/phases/05-weight-push-learning-loop/05-RESEARCH.md (D-08 soak methodology; Real-receiver POST wiring — awaited fetch, not fire-and-forget, so persistence round-trips before the "after" read)
    - .planning/phases/05-weight-push-learning-loop/05-PATTERNS.md (admin/soak-test-weights.mjs section — header/framing, margin helper reuse, real-receiver POST wiring)
  </read_first>
  <action>
    Create admin/soak-test-weights.mjs as a dev-only ES-module script with a header comment matching admin/print-softmax-margins.mjs's framing (dev-only, run via `node admin/soak-test-weights.mjs` / `npm run soak-test`, NEVER imported by src/, requires `npm run receiver` running). Import `{ forwardPass, initInference, endSession }` from '../src/inference.js', the bus publish/subscribe entry the tests use to seed signal:detected (mirror tests/inference-endsession.test.js's import), and the cold-start weights from './weights.js'. Re-declare the same module-scoped `CLASSES` and `SIGNAL_ORDER` constants as print-softmax-margins.mjs, and the same CANONICAL_VECTORS (the 4 canonical one-signal-active input vectors + labels). Reuse argmax()/margin()/printVector() helpers verbatim, but generalize printVector to accept an explicit weights argument (so it can print with cold-start weights for "before" and with persisted weights for "after"). Read the receiver URL from a `const RECEIVER_URL = 'http://localhost:4310/weights'` constant (matching config/demo-platform.json and 05-04's harness). BEFORE the loop: print + record the "before" margins for all 4 CANONICAL_VECTORS using cold-start weights. Then run a loop of 10-20 sessions (pick a fixed count in that range): each iteration (a) build `config = { inference: { weights: currentWeights, confidenceThreshold: ... } }`, (b) initInference(config), (c) seed lastInference by publishing a synthetic signal:detected whose type varies across the 4 signal types round-robin (varied signal), (d) call `const updated = endSession(config, outcome)` with outcome (flowComplete) alternating true/false across sessions (varied outcome), (e) `await fetch(RECEIVER_URL, { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(updated) })` and assert the response is ok (fail loudly with a clear message + exit(1) if the receiver is unreachable — the soak test REQUIRES the real receiver per D-08), (f) set `currentWeights = updated` for the next iteration so learned weights feed forward. AFTER the loop: print the "after" margins for all 4 CANONICAL_VECTORS using `currentWeights`. Apply the SC3 gate mirroring print-softmax-margins.mjs: for each canonical vector's after-result, `notSaturated = topProb < 0.98` AND `notCollapsed = margin >= 0.02` (a real, non-uniform separation — ~0.25-each uniform gives margin ≈ 0); on any failure console.error a `GATE FAIL` line and set allPass=false. End with `if (allPass) { console.log('PASS ...'); process.exit(0); } else { console.error('FAIL ...'); process.exit(1); }`.
  </action>
  <verify>
    <automated>node local-receiver/server.js & RECV=$!; sleep 1; node admin/soak-test-weights.mjs; RC=$?; kill $RECV 2>/dev/null; exit $RC</automated>
  </verify>
  <acceptance_criteria>
    - admin/soak-test-weights.mjs imports forwardPass/initInference/endSession from ../src/inference.js and cold-start weights from ./weights.js; reuses argmax/margin/printVector helpers from the print-softmax-margins analog.
    - The loop runs 10-20 sessions, varies the active signal type AND the outcome across sessions, and `await`s a POST to http://localhost:4310/weights each session (fails loudly with exit(1) if the receiver is unreachable).
    - It prints softmax margins for all 4 canonical signals BEFORE (cold-start) and AFTER (post-soak) the run.
    - SC3 gate present: exits 0 only when no canonical signal saturates (topProb < 0.98) or collapses to uniform (margin >= 0.02); exits 1 otherwise.
    - `grep -nE "require\(|from ['\"](express|node-fetch|axios)" admin/soak-test-weights.mjs` returns nothing (Node global fetch + existing src imports only, D-04).
    - With the receiver running, `node admin/soak-test-weights.mjs` exits 0 and prints a PASS line.
  </acceptance_criteria>
  <done>admin/soak-test-weights.mjs runs the full synthetic soak through the real receiver and gates SC3 (no softmax collapse/saturation) with a process.exit(0/1) result.</done>
</task>

<task type="auto">
  <name>Task 2: SC2 GET-readback assertion — persisted weights are what a restart loads</name>
  <files>admin/soak-test-weights.mjs</files>
  <read_first>
    - admin/soak-test-weights.mjs (Task 1's script — the AFTER phase where currentWeights holds the last-pushed update and the receiver holds the persisted file)
    - .planning/phases/05-weight-push-learning-loop/05-RESEARCH.md (Open Question #2 — SC2 verified via the soak-test's before/after GET comparison, resolved in Plan 05-04's sc2_verification_decision block; D-06 receiver-side GET re-validation)
    - src/inference.js validateWeightsShape / the cold-start weights shape (to compare persisted-vs-cold-start for the "differs from cold-start" assertion)
  </read_first>
  <action>
    Extend the AFTER phase of admin/soak-test-weights.mjs to add the SC2 gate: after the soak loop, `await fetch(RECEIVER_URL)` (GET) to read the persisted weights back through the receiver's exact GET /weights endpoint — the same endpoint the harness bootstrap (05-04) uses at cold start — and `const persisted = await res.json()`. Assert (a) the GET response is ok and `persisted` is a valid {W1,b1,W2,b2} numeric shape (fail with exit(1) + a clear message otherwise), (b) `persisted` DIFFERS from the cold-start weights imported from ./weights.js (at least one weight value changed — deep-compare or a stringified inequality), proving a restart would NOT reload cold-start defaults, and (c) `forwardPass(canonicalInput, persisted)` for each canonical signal produces the SAME (within a tight tolerance) margins as the "after" print computed from `currentWeights` — proving the receiver persisted and re-served exactly the learned weights the loop produced (the persistence round-trip is lossless). Fold these into the same allPass gate so an SC2 failure also drives process.exit(1). Add a header/inline comment stating this block is the deterministic SC2 verification (restart loads learned, not cold-start weights — RESEARCH Open Question #2 resolution). Do NOT re-run the soak loop; this task only reads back and asserts against Task 1's already-persisted state.
  </action>
  <verify>
    <automated>node local-receiver/server.js & RECV=$!; sleep 1; node admin/soak-test-weights.mjs; RC=$?; kill $RECV 2>/dev/null; exit $RC</automated>
  </verify>
  <acceptance_criteria>
    - After the loop the script GETs http://localhost:4310/weights and validates the returned body is a {W1,b1,W2,b2} numeric shape.
    - It asserts the persisted weights DIFFER from the cold-start weights in ./weights.js (SC2 — a restart would load learned weights).
    - It asserts forwardPass against the GET-read persisted weights matches the "after" margins computed from the in-memory last update (lossless round-trip).
    - Any SC2 assertion failure drives process.exit(1); a full pass (SC3 + SC2) prints PASS and exits 0.
    - No new npm dependency added (grep confirms only Node global fetch + existing src imports).
  </acceptance_criteria>
  <done>The soak-test reads persisted weights back through the receiver GET endpoint and proves they are learned (non-cold-start) and losslessly round-tripped — the deterministic SC2 gate.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- New file: `admin/soak-test-weights.mjs` — dev-only Node script (run via `npm run soak-test`, wired in 05-02). Drives 10-20 synthetic sessions through endSession() + the real receiver, prints before/after canonical-signal softmax margins, gates SC3 (no collapse/saturation) and SC2 (GET-readback proves persisted weights are learned + losslessly round-tripped), and exits 0/1.
- No new symbols exported; no new npm dependencies (Node global fetch + existing src/inference.js/src/bus.js imports only).
</artifacts>

<sc2_verification_note>
## SC2 verification (resolves RESEARCH Open Question #2)
This plan is the chosen concrete SC2 gate: the soak-test's after-run GET-readback (Task 2) reads persisted weights back through the receiver's GET /weights endpoint — the same endpoint the 05-04 harness bootstrap fetches — and asserts they differ from cold-start and round-trip losslessly. A live-receiver Playwright e2e was deliberately NOT chosen (playwright.config.js has no webServer block, navigates file:// with an opaque origin, and would need a debug hook for the active weights vector — flaky and heavyweight). SC3 is verified here too (D-08 script gate). Both are manual/CI script gates per 05-VALIDATION.md's Manual-Only Verifications table, not Vitest assertions.
</sc2_verification_note>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| soak-test process → receiver | The script POSTs synthetic weight updates to and GETs persisted weights from the local receiver over loopback — exercising the same untrusted-input boundary the browser harness does. |
| receiver → OS filesystem | The receiver (05-02) owns the atomic write; the soak-test only drives it via HTTP, never touches the weights file directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Tampering | soak-test weight bodies persisted via receiver | low | mitigate | The receiver re-validates every POST body's {W1,b1,W2,b2} shape before writing and re-validates on GET (05-02, D-06); the soak-test's own SC2 readback also asserts the persisted shape — a malformed synthetic update is rejected (4xx) rather than corrupting the persisted file. |
| T-05-10 | Denial of Service | soak-test flooding the receiver | low | accept | 10-20 sequential awaited POSTs of a ~1-2KB payload against a local single-writer receiver — well within the receiver's MAX_BODY_BYTES cap; local dev tooling only, no concurrency hazard (D-04 scope). |
| T-05-11 | Information Disclosure | persisted synthetic weights | low | mitigate | Bodies are numeric {W1,b1,W2,b2} only — no PII, no field values, no identity (CLAUDE.md No-PII); the persisted file is gitignored (05-02). |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages — Node global fetch + existing src imports only (D-04). No install step, no supply-chain surface. |
</threat_model>

<verification>
- With `npm run receiver` running, `node admin/soak-test-weights.mjs` (or `npm run soak-test`) prints before/after canonical-signal margins and exits 0 on a healthy loop.
- SC3: after 10-20 sessions no canonical signal collapses to uniform (margin >= 0.02) or saturates (topProb < 0.98) — else exit 1.
- SC2: the GET-readback confirms persisted weights differ from cold-start and losslessly reproduce the last in-memory update's margins.
- `grep` confirms no new npm dependency (Node global fetch + existing src imports only).
- The soak-test performs no real network I/O beyond the intended loopback receiver calls, and is never imported by src/ (dev-tooling boundary, matching admin/print-softmax-margins.mjs).
</verification>

<success_criteria>
admin/soak-test-weights.mjs proves, deterministically and browser-free, that the weight-push loop is stable across 10-20 sessions (SC3) and that persisted learned weights are exactly what a cold-start restart would load (SC2), gating both with process.exit(0/1) against the real receiver.
</success_criteria>

<output>
Create `.planning/phases/05-weight-push-learning-loop/05-05-SUMMARY.md` when done.
</output>
