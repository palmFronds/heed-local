---
phase: 05-weight-push-learning-loop
plan: 03
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - src/inference.js
  - src/log.js
  - src/index.js
autonomous: true
requirements: [WEIGHT-01]

must_haves:
  truths:
    - "endSession() returns the updated activeWeights object so the logging layer has something to push (SC1, Pitfall 1)."
    - "At session end, src/log.js POSTs the updated weights to config.weightPushUrl — via fetch() on the flow:complete path and navigator.sendBeacon() on the pagehide/abandon path (D-03)."
    - "A failed or absent-URL push never throws into the host page (best-effort, no-crash discipline)."
    - "initDemo(overrides) injects overrides.weights into config.inference.weights before init() runs; a bare initDemo() is byte-identical in behavior to before (SC2 source, SC4 harness-side, Pitfall 2)."
  artifacts:
    - src/inference.js
    - src/log.js
    - src/index.js
  key_links:
    - "src/log.js reads weightPushUrl live off activeConfig (already re-resolved every initLogging call) — no new module-state variable, and the push is guarded by `if (updatedWeights && activeConfig.weightPushUrl)` so an absent URL no-ops (D-06/A3)."
    - "pushWeights() is a single choke-point function mirroring writeLog()'s discipline; the transport split (fetch vs sendBeacon) is the ONLY branch (D-03)."
    - "No change to forwardPass, buildTarget, gradientStep, confidence gate, or the INF-05 fallback chain (config.inference?.weights ?? coldStartWeights) — phase boundary is explicit (D-07)."
---

<objective>
Wire the SDK's session-end weight push and the harness cold-start injection hook: add the one-line `return activeWeights;` to endSession(), add the fetch/sendBeacon transport split to src/log.js's finishSession() via a single-choke-point pushWeights(), and give initDemo() an optional weights-override parameter. Flips the endSession "returns" and initDemo-override/cold-start RED cases to GREEN.

Purpose: The SDK-side half of the loop — transmit learned weights out, and accept injected learned weights in.
Output: additive changes to src/inference.js, src/log.js, src/index.js — no math touched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-weight-push-learning-loop/05-RESEARCH.md
@.planning/phases/05-weight-push-learning-loop/05-PATTERNS.md
@src/inference.js
@src/log.js
@src/index.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: endSession() returns updated weights (Pitfall 1, additive)</name>
  <files>src/inference.js</files>
  <read_first>
    - src/inference.js endSession() (lines ~281-304 — the current body ending in the gradientStep assignment with no return)
    - .planning/phases/05-weight-push-learning-loop/05-PATTERNS.md (src/inference.js endSession section — the exact additive one-line change)
    - .planning/phases/05-weight-push-learning-loop/05-RESEARCH.md (Pitfall 1 / Architecture Pattern 2)
    - tests/inference-endsession.test.js (the RED "returns" case this satisfies, plus the existing INF-04 cases that must stay green)
  </read_first>
  <behavior>
    - After a signal has fired and endSession(config, outcome) runs, it returns the {W1,b1,W2,b2} object equal to the post-update activeWeights the next forwardPass reads.
    - The no-signal path (lastInference null) still returns undefined so callers can guard on it.
    - The non-boolean-outcome path still throws (buildTarget runs before the return) — WR-02 regression stays green.
  </behavior>
  <action>
    Add exactly one line — `return activeWeights;` — as the final statement of endSession(), after the `activeWeights = gradientStep(...)` assignment, with a short why-comment (src/log.js needs the updated weights to POST — SC1/Pitfall 1). Do NOT touch buildTarget, gradientStep, forwardPass, the confidence gate, the learning-rate literal, or the lastInference-reset semantics. The early `if (!lastInference) return;` stays as-is (returns undefined — the guarded no-op path). This is the phase's one sanctioned edit inside src/inference.js (D-07 framing).
  </action>
  <verify>
    <automated>npx vitest run tests/inference-endsession.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "return activeWeights" src/inference.js` is 1.
    - tests/inference-endsession.test.js fully green, including the new "returns" case AND all pre-existing INF-04 cases (no-op path, independent-step, WR-03 reset, WR-02 throw).
    - No diff to gradientStep/buildTarget/forwardPass function bodies (git diff scoped to the endSession return line + comment).
  </acceptance_criteria>
  <done>endSession() exposes its computed weights via a single additive return; all inference tests green; math untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Weight-push transport split in src/log.js (D-03)</name>
  <files>src/log.js</files>
  <read_first>
    - src/log.js (full file — writeLog() choke-point discipline lines 51-67, finishSession() lines 69-82, the No-PII firewall header, the activeConfig/sessionEnded module-state pattern)
    - .planning/phases/05-weight-push-learning-loop/05-PATTERNS.md (src/log.js finishSession + pushWeights section — exact shape)
    - .planning/phases/05-weight-push-learning-loop/05-RESEARCH.md (Architecture Pattern 2 pushWeights; Pitfall 3 sendBeacon text/plain; D-03 transport rationale)
    - config/demo-platform.json (weightPushUrl top-level — the value pushWeights reads off activeConfig)
  </read_first>
  <behavior>
    - finishSession(true, 'flow_complete') captures endSession's return and, if truthy and activeConfig.weightPushUrl is set, POSTs it via fetch().
    - finishSession(false, 'flow_abandoned') pushes via navigator.sendBeacon() instead (unload-safe transport).
    - When weightPushUrl is absent, or endSession returned undefined (no signal fired), no push is attempted and nothing throws.
    - A rejected fetch() is swallowed (.catch) — a failed push never breaks the host page.
  </behavior>
  <action>
    In finishSession(), capture `const updatedWeights = endSession(activeConfig, outcome);` (replacing the current bare `endSession(activeConfig, outcome);` call) and add, guarded, `if (updatedWeights && activeConfig.weightPushUrl) pushWeights(activeConfig.weightPushUrl, updatedWeights, event === 'flow_abandoned');`. Add a new module-scoped single-choke-point function `pushWeights(url, weights, useBeacon)` with a why-comment mirroring writeLog()'s convention: it JSON.stringifies the weights once; if useBeacon it calls `navigator.sendBeacon(url, body)` (string payload → text/plain → no CORS preflight, D-03); else it calls `fetch(url, { method:'POST', headers:{'Content-Type':'application/json'}, body }).catch(() => {})` (best-effort, never throw into the host page). Read weightPushUrl live off activeConfig — do NOT add a new module-state variable (mirrors the existing activeConfig/activeSessionId live-read pattern). endSession is already imported; fetch/navigator are ambient globals (no import). Keep the No-PII firewall header comment accurate — the pushed body is weight numbers only, never DOM/host data.
  </action>
  <verify>
    <automated>npx vitest run tests/log.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function pushWeights" src/log.js` is 1; it is the only site calling `navigator.sendBeacon` and the only new `fetch(` call in the file.
    - finishSession captures endSession's return and guards the push on `updatedWeights && activeConfig.weightPushUrl`.
    - The fetch branch has a `.catch` (no unhandled rejection can reach the host page).
    - tests/log.test.js green (existing D-01/02/03 session-lifecycle behavior preserved); any new push-path assertion mocks fetch/sendBeacon via vi.stubGlobal (no real network — Pitfall 4).
  </acceptance_criteria>
  <done>src/log.js POSTs updated weights at session end with the D-03 fetch/sendBeacon split via a single guarded choke-point that never throws into the host page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: initDemo(overrides) weights injection (Pitfall 2)</name>
  <files>src/index.js</files>
  <read_first>
    - src/index.js initDemo() (lines ~51-58 — current zero-arg wrapper + stale "no backend/fetch" comment) and init() (the function it delegates to, whose signature/return must NOT change)
    - .planning/phases/05-weight-push-learning-loop/05-PATTERNS.md (src/index.js initDemo section — exact additive override shape + stale-comment note)
    - .planning/phases/05-weight-push-learning-loop/05-RESEARCH.md (Pattern 1; Pitfall 2; Assumption A2 — inject via override param, not a duplicated harness config)
    - tests/index.test.js (the RED "initDemo override" + "cold-start fallback" cases this satisfies; the existing init() cases that must stay green)
  </read_first>
  <behavior>
    - initDemo({ weights }) returns init() run against a config whose inference.weights is the injected object (shallow-merged over demoConfig, not mutating demoConfig).
    - initDemo() and initDemo({}) behave exactly as the old zero-arg initDemo() did (cold-start path, no weights injected) — the existing Playwright suite calls it bare and must keep passing.
  </behavior>
  <action>
    Change initDemo to accept an optional `overrides` parameter. When `overrides?.weights` is present, build a NON-mutating merged config `{ ...demoConfig, inference: { ...demoConfig.inference, weights: overrides.weights } }` and pass it to init(); otherwise pass demoConfig unchanged. Update the JSDoc block and the stale inline comment that claims "the harness needs no backend/fetch to run" — after D-01 the harness DOES fetch cold-start weights (in its own bootstrap, not in the SDK); reword to note the override is supplied by the caller, the SDK bundle itself still makes no cold-start GET (D-01). Do not change init()'s signature or its `{ config, publish, subscribe }` return shape.
  </action>
  <verify>
    <automated>npx vitest run tests/index.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function initDemo(overrides)" src/index.js` is 1.
    - The merge does not mutate demoConfig (spread copy — grep confirms `{ ...demoConfig` in initDemo).
    - tests/index.test.js green: "initDemo override" injects weights, "cold-start fallback" no-throws with weights unset, and the two original init() cases still pass.
    - The stale "no backend/fetch" comment is reworded (grep the old exact phrase no longer describes SDK-side fetch inaccurately).
  </acceptance_criteria>
  <done>initDemo(overrides) injects learned weights into config before init() runs while preserving byte-identical zero-arg behavior; SC2/SC4 harness-injection contract is met at the SDK boundary.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- Changed export: `endSession(config, outcome)` in src/inference.js now returns the updated `{W1,b1,W2,b2}` (or undefined on the no-signal path).
- New function: `pushWeights(url, weights, useBeacon)` in src/log.js (module-scoped choke-point; fetch vs sendBeacon transport split).
- Changed behavior in `finishSession()` (src/log.js): captures endSession's return and pushes to `activeConfig.weightPushUrl`.
- Changed export: `initDemo(overrides)` in src/index.js accepts `{ weights }` and injects into `config.inference.weights`.
- No new npm dependencies; no build artifact committed (dist/ is gitignored — rebuilt in Plan 05-04).
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK (browser) → receiver | The only outbound network call the shipped SDK makes is the session-end weight push (CLAUDE.md "one outbound call") — payload is weight numbers only. |
| caller/harness → initDemo | Injected weights are shallow-merged into config; init()'s existing validateWeightsShape() (Phase 3) validates any injected weights before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Information Disclosure | pushWeights() body | low | mitigate | Body is JSON.stringify(weights) — numeric {W1,b1,W2,b2} only, never DOM/host/PII (CLAUDE.md No-PII); this is the single sanctioned outbound SDK call. |
| T-05-05 | Denial of Service | failed push breaking host page | low | mitigate | fetch push is `.catch(()=>{})` best-effort and guarded by `if (updatedWeights && weightPushUrl)`; sendBeacon is fire-and-forget — a receiver being down never throws into the host page. |
| T-05-02 | Tampering | injected cold-start weights | low | mitigate | initInference's existing validateWeightsShape() hard-fails on a malformed injected weights object (Phase 3, unchanged) — the harness-side guard (Plan 05-04) ensures only well-formed or absent weights are ever injected (D-06/D-07). |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages (ambient browser globals + existing imports only) — no supply-chain surface. |
</threat_model>

<verification>
- `npx vitest run tests/inference-endsession.test.js tests/log.test.js tests/index.test.js` all green.
- `npm test` full Vitest suite green — no regression in signal/inference/response/log suites.
- git diff confined to endSession's return line, log.js finishSession+pushWeights, and index.js initDemo — no forward-pass/gradient math touched.
</verification>

<success_criteria>
endSession exposes its weights, log.js pushes them with the correct per-lifecycle transport without ever throwing into the host page, and initDemo(overrides) injects learned weights before init() — all via additive changes, math and fallback chain untouched.
</success_criteria>

<output>
Create `.planning/phases/05-weight-push-learning-loop/05-03-SUMMARY.md` when done.
</output>
