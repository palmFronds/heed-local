---
phase: 05-weight-push-learning-loop
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/local-receiver.test.js
  - tests/inference-endsession.test.js
  - tests/index.test.js
autonomous: true
requirements: [WEIGHT-01]

must_haves:
  truths:
    - "A RED unit suite exists asserting the receiver persists a valid POST and rejects a malformed one (SC1/SC4) — failing only because local-receiver/server.js does not exist yet."
    - "A RED case exists asserting endSession() returns the updated activeWeights object (SC1 source, Pitfall 1) — failing only because the return statement is not added yet."
    - "A RED case exists asserting initDemo(overrides) injects config.inference.weights before init() runs, and that initDemo() with no argument still cold-starts (SC2 source, SC4 harness-side, Pitfall 2)."
  artifacts:
    - tests/local-receiver.test.js
    - tests/inference-endsession.test.js
    - tests/index.test.js
  key_links:
    - "tests/local-receiver.test.js MUST carry a `// @vitest-environment node` first-line override so it runs under Node's real http/fs semantics, not the project-default happy-dom (RESEARCH Pitfall 4)."
    - "Every new fetch/sendBeacon-touching case mocks the global via vi.stubGlobal — no real network I/O in Vitest."
---

<objective>
Author the Wave-0 RED test scaffolding for Phase 5 so every WEIGHT-01 success criterion has a failing assertion before any implementation lands. This encodes SC1 (receiver persist), SC4 (receiver malformed/corrupt handling + harness-side fallback), the endSession() return-value gap (Pitfall 1), and the initDemo(overrides) injection gap (Pitfall 2).

Purpose: Nyquist compliance — no implementation task in Waves 1-2 lands without a pre-existing test that flips RED→GREEN.
Output: One new test file plus additive cases in two existing test files, all RED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-weight-push-learning-loop/05-RESEARCH.md
@.planning/phases/05-weight-push-learning-loop/05-VALIDATION.md
@.planning/phases/05-weight-push-learning-loop/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED receiver unit suite (SC1 + SC4 receiver-side)</name>
  <files>tests/local-receiver.test.js</files>
  <read_first>
    - tests/inference-endsession.test.js (assertion + module-import style to mirror)
    - .planning/phases/05-weight-push-learning-loop/05-RESEARCH.md (Architecture Pattern 3 = receiver shape under test; Pitfall 4 = why node env; Validation Architecture → Phase Requirements → Test Map for the exact -t names)
    - admin/weights.js (the exact {W1,b1,W2,b2} valid-weights fixture shape)
    - src/inference.js lines 97-111 area (validateWeightsShape — the shape the receiver's isValidWeights must mirror)
  </read_first>
  <action>
    Create tests/local-receiver.test.js. The VERY FIRST line MUST be the per-file environment override comment selecting Node's environment (RESEARCH Pitfall 4) — without it the suite runs under happy-dom and any fetch performs real network I/O. Import `{ createReceiver }` from ../local-receiver/server.js (does not exist yet — this import is what makes the suite RED). In a beforeEach/afterEach, start the server via `createReceiver({ weightsPath: <a fresh temp file path under os.tmpdir()> })` and `.listen(0)` (ephemeral port), reading the bound port from `server.address().port`; close it in afterEach and remove the temp file. Author these cases with the -t names the Test Map fixes: (a) "POST persists" — POST a valid {W1,b1,W2,b2} body to /weights, assert 200 + the temp file now contains the posted object (round-trip via fs.readFileSync + JSON.parse); (b) "malformed POST" — POST an invalid body (non-JSON string AND a shape-invalid JSON object in separate assertions), assert the response status is 400, the server process stays up (a following valid request still succeeds), and the temp weights file is NOT written; (c) "corrupt file" — pre-write a corrupt/garbage string into the temp weights path, GET /weights, assert the receiver does not crash and does not serve the garbage (non-200 or last-known-good), then confirm a subsequent valid POST + GET returns the valid object. Use Node's global fetch for requests. Reference SC1/SC4 and D-06 in a header comment.
  </action>
  <verify>
    <automated>npx vitest run tests/local-receiver.test.js 2>&1 | grep -Ei "cannot find|failed|error" ; echo "expected RED: import of ../local-receiver/server.js fails to resolve"</automated>
  </verify>
  <acceptance_criteria>
    - First line of tests/local-receiver.test.js is the Node environment override directive (grep confirms `@vitest-environment node` present on line 1).
    - Suite imports `{ createReceiver }` from ../local-receiver/server.js and fails to resolve (RED) — not a passing suite.
    - Cases named to match `-t "POST persists"`, `-t "malformed POST"`, `-t "corrupt file"`.
    - No literal outbound host (localhost hard-coded production URL) — uses the ephemeral bound port from server.address().
  </acceptance_criteria>
  <done>tests/local-receiver.test.js exists, is RED due to the missing server module, and covers SC1 persist + SC4 malformed-POST + SC4 corrupt-file behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: RED case — endSession() returns updated weights (SC1 source, Pitfall 1)</name>
  <files>tests/inference-endsession.test.js</files>
  <read_first>
    - tests/inference-endsession.test.js (full file — the existing INF-04 suite and its WEIGHTS fixture / helpers to reuse)
    - src/inference.js endSession() (lines ~297-304 — currently returns undefined)
    - .planning/phases/05-weight-push-learning-loop/05-RESEARCH.md (Pitfall 1 / Architecture Pattern 2)
  </read_first>
  <action>
    Add one additive test case to the existing `describe('INF-04', ...)` block (or a new sibling describe) named to match `-t "returns"`. Reusing the file's existing WEIGHTS fixture, touchHesitationSignal(), and initInference(config): initInference, publish a signal:detected to seed lastInference, then assert `endSession(config, false)` RETURNS an object with numeric W1/b1/W2/b2 (not undefined), and that the returned object is the same post-update activeWeights the next forwardPass reads (assert a probs value computed from the returned weights matches the next inference:result). Do NOT alter any existing case. This case is RED until Plan 05-03 adds `return activeWeights;`.
  </action>
  <verify>
    <automated>npx vitest run tests/inference-endsession.test.js -t "returns" 2>&1 | grep -Ei "fail|expected|received" ; echo "expected RED until 05-03"</automated>
  </verify>
  <acceptance_criteria>
    - New case name contains the substring "returns".
    - Case asserts endSession() return is a {W1,b1,W2,b2} object, currently failing (return is undefined).
    - All previously-passing INF-04 cases remain untouched (byte-identical) and still green.
  </acceptance_criteria>
  <done>tests/inference-endsession.test.js has a RED "returns" case pinning Pitfall 1's fix; existing cases unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: RED cases — initDemo(overrides) injection + cold-start fallback (SC2/SC4, Pitfall 2)</name>
  <files>tests/index.test.js</files>
  <read_first>
    - tests/index.test.js (full file — existing init() suite style)
    - src/index.js initDemo() (currently zero-arg, ignores arguments)
    - config/demo-platform.json (demo config has inference.confidenceThreshold, NO weights — the fallback baseline)
    - .planning/phases/05-weight-push-learning-loop/05-RESEARCH.md (Pattern 1, Pitfall 2, Validation Test Map -t names)
  </read_first>
  <action>
    Add additive cases to tests/index.test.js matching the Test Map -t names: (1) `-t "initDemo override"` — call `initDemo({ weights: <valid {W1,b1,W2,b2} fixture> })` and assert the returned config.inference.weights equals the injected fixture (proving injection into config before init runs), while a bare `initDemo()` returns a config whose inference has NO injected weights (cold-start path); (2) `-t "cold-start fallback"` — assert `initDemo()` and `initDemo({})` (no weights key) do NOT throw and both yield a config that initInference will resolve to cold-start weights (config.inference.weights is undefined). Import a valid weights fixture inline. These are RED until Plan 05-03 adds the overrides parameter to initDemo(). Keep the existing two init() cases untouched.
  </action>
  <verify>
    <automated>npx vitest run tests/index.test.js -t "initDemo override" 2>&1 | grep -Ei "fail|expected|received" ; echo "expected RED until 05-03"</automated>
  </verify>
  <acceptance_criteria>
    - New case names contain "initDemo override" and "cold-start fallback".
    - "initDemo override" asserts injected weights land in returned config.inference.weights — currently failing (initDemo ignores its argument).
    - "cold-start fallback" asserts no-arg / empty-overrides paths do not throw and leave weights unset.
    - Existing init() cases unchanged and still green.
  </acceptance_criteria>
  <done>tests/index.test.js has RED cases pinning the initDemo(overrides) injection + zero-arg fallback contract; existing cases unchanged.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- New file: `tests/local-receiver.test.js` (Node-env RED suite; cases `POST persists`, `malformed POST`, `corrupt file`)
- Extended file: `tests/inference-endsession.test.js` (new RED case matching `returns`)
- Extended file: `tests/index.test.js` (new RED cases `initDemo override`, `cold-start fallback`)
- No production symbols created this plan (test authorship only).
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → OS filesystem/network | RED tests spin a local server on an ephemeral port and write to an os.tmpdir() temp file — no production path, no real remote host. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-TEST-01 | Tampering | test-suite real-network leakage | low | mitigate | `// @vitest-environment node` on line 1 of local-receiver.test.js + vi.stubGlobal for any fetch/sendBeacon in happy-dom suites (RESEARCH Pitfall 4) — no test performs unintended real outbound I/O. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (zero-dependency, Node built-ins only per D-04) — no supply-chain surface introduced. |
</threat_model>

<verification>
- `npx vitest run tests/local-receiver.test.js` is RED (missing module import).
- `npx vitest run tests/inference-endsession.test.js -t "returns"` is RED.
- `npx vitest run tests/index.test.js -t "initDemo override"` is RED.
- Full existing suite (`npm test`) shows only the newly-added cases failing; no previously-green case regresses.
</verification>

<success_criteria>
All three RED assertions exist and fail for the intended (not-yet-implemented) reason, with `// @vitest-environment node` on line 1 of the receiver suite and no real network calls in any happy-dom suite.
</success_criteria>

<output>
Create `.planning/phases/05-weight-push-learning-loop/05-01-SUMMARY.md` when done.
</output>
