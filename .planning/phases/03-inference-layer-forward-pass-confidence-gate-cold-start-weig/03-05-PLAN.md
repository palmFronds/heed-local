---
phase: 03-inference-layer-forward-pass-confidence-gate-cold-start-weig
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-03", "03-04"]
files_modified:
  - src/index.js
  - admin/check-bundle-purity.mjs
  - admin/print-softmax-margins.mjs
autonomous: false
requirements: [INF-01, INF-02, INF-03, INF-05]
must_haves:
  truths:
    - "src/index.js's init() wires initInference(config) after initSignalCapture(config), preserving hard-fail-first ordering, with no new key added to init()'s return shape"
    - "dist/sdk.js (esbuild output) contains no brain.js internals -- brain.js never reaches the shipped runtime bundle"
    - "The 4 canonical cold-start mappings each classify correctly with a real (non-saturated, non-uniform) softmax margin, and one ambiguous blended input demonstrates genuine uncertainty -- Success Criterion 2's explicit acceptance gate"
  artifacts:
    - admin/check-bundle-purity.mjs
    - admin/print-softmax-margins.mjs
    - "src/index.js (modified -- initInference wiring)"
  key_links:
    - "src/index.js -> src/inference.js's initInference is the final reachability link making INF-01/02/03/05 actually exercised end-to-end via a real init() call, not just unit-tested in isolation"
    - "admin/print-softmax-margins.mjs imports admin/weights.js (plan 03-01) and src/inference.js's forwardPass (plan 03-03) -- both must already be stable"
---

<objective>
Wire the inference layer into the SDK's real `init()` entry point, confirm brain.js never leaks into
the shipped `dist/sdk.js` bundle, and give Success Criterion 2 (softmax margin quality for the 4
canonical mappings plus one ambiguous input) its own explicit, numerically-gated verification —
closing out this phase, the deepest-treatment phase in the roadmap.

Purpose: integration + the phase's own acceptance gate — the forward pass and confidence gate are
only real once they're reachable from a live `init()` call, and Success Criterion 2's "real margin,
not saturated or uniform" requirement needs a concrete, human-readable check, not an implicit
assertion buried in a unit test.
Output: src/index.js wired to initInference; admin/print-softmax-margins.mjs; full suite green; human-verify phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-CONTEXT.md
@.planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-RESEARCH.md
@.planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-PATTERNS.md
@.planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-VALIDATION.md
@src/index.js
@src/inference.js
@admin/weights.js
</context>

## Artifacts this phase produces (this plan's slice)

- `src/index.js` (modified) — imports and calls `initInference(config)` inside `init()`.
- New file `admin/check-bundle-purity.mjs` — dev-only Node ESM script; exits nonzero if `dist/sdk.js` contains brain.js internals (`NeuralNetworkGPU`/`thaw`).
- New file `admin/print-softmax-margins.mjs` — dev-only Node ESM script; prints and numerically gates the 5 softmax-margin stress vectors (4 canonical + 1 ambiguous) required by Success Criterion 2.

<tasks>

<task type="auto">
  <name>Task 1: Wire initInference into src/index.js's init(); verify brain.js never reaches dist/sdk.js</name>
  <files>src/index.js, admin/check-bundle-purity.mjs</files>
  <read_first>
    - src/index.js (current full file — the exact validateConfig -> initSignalCapture -> return ordering and why-comment convention this task extends)
    - .planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-PATTERNS.md ("src/index.js (modified — orchestrator/wiring)" section)
    - .planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-RESEARCH.md (Common Pitfall #4 — brain.js bundle-bloat warning signs)
    - package.json (existing "build" script — exact esbuild invocation this task runs to produce dist/sdk.js)
  </read_first>
  <action>
    In src/index.js, add an import of initInference from ./inference.js alongside the existing
    initSignalCapture import. Inside init(rawConfig), call initInference(config) immediately after
    initSignalCapture(config), preserving the exact hard-fail-first ordering (validateConfig throws
    first; nothing below it runs against a partially-valid config). Add a why-comment parallel to the
    existing one on initSignalCapture, explaining initInference is wired the same way — never against
    an unvalidated config. Do NOT add any new key to init()'s return object — it stays exactly
    { config, publish, subscribe }. Then create admin/check-bundle-purity.mjs, a small standalone
    dev-only Node ESM script that reads dist/sdk.js from disk and exits 1 with an error message if it
    contains the string NeuralNetworkGPU or the string thaw (brain.js's own dependency, thaw.js),
    exits 0 with a "bundle clean" message and the byte size otherwise. Run the existing npm run build
    script (esbuild src/index.js --bundle --format=iife --global-name=Heed --outfile=dist/sdk.js)
    followed by node admin/check-bundle-purity.mjs to confirm the produced dist/sdk.js contains none
    of brain.js's characteristic internals — confirming src/inference.js's cold-start weight loading
    (which imports the plain data module admin/weights.js, not brain.js itself) never pulled brain.js's
    ~1MB+ UMD bundle into the shipped runtime artifact.
  </action>
  <verify>
    <automated>npm run build && node admin/check-bundle-purity.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes and produces `dist/sdk.js`.
    - `node admin/check-bundle-purity.mjs` exits 0 — `dist/sdk.js` contains no occurrence of `NeuralNetworkGPU` or `thaw`.
    - Source assertion: `src/index.js` imports `initInference` from `./inference.js` and calls it inside `init()` immediately after `initSignalCapture(config)`.
    - Source assertion: `init()`'s return statement is unchanged — still exactly `{ config, publish, subscribe }`.
    - `npx vitest run` (full suite) stays green after the wiring change.
  </acceptance_criteria>
  <done>initInference is reachable from the SDK's real init() entry point, in the correct hard-fail-first order, and the shipped dist/sdk.js bundle is confirmed free of brain.js internals via admin/check-bundle-purity.mjs.</done>
</task>

<task type="auto">
  <name>Task 2: Build admin/print-softmax-margins.mjs — Success Criterion 2's explicit margin-quality gate</name>
  <files>admin/print-softmax-margins.mjs</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 3 Success Criterion 2's exact wording — "printing the full softmax vector for each of the 4 canonical cold-start mappings... plus one deliberately ambiguous input shows the correct class winning with a real margin — not a saturated (~1.0) or uniform (~0.25 each) distribution")
    - .planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-RESEARCH.md (Assumption A2 — 70/30 ambiguous blend recommendation, not exactly 50/50; the empirically observed 0.21-0.35 margin range for the canonical recipe)
    - admin/weights.js (plan 03-01's generated default export shape)
    - src/inference.js (plan 03-03/03-04 — forwardPass, CLASSES, SIGNAL_ORDER exports)
  </read_first>
  <action>
    Create admin/print-softmax-margins.mjs as a dev-only Node ESM script (never imported by src/ or
    bundled into dist/sdk.js) importing forwardPass from ../src/inference.js and the default weights
    export from ./weights.js. Construct 5 stress inputs in SIGNAL_ORDER: the 4 canonical one-hot
    vectors ([1,0,0,0] touch_hesitation, [0,1,0,0] blur_incomplete, [0,0,1,0] scroll_reversal,
    [0,0,0,1] back_intent) plus one deliberately ambiguous blended vector [0.7, 0.3, 0, 0] (a 70/30
    blend of two conflicting canonical signals, per RESEARCH.md Assumption A2 — NOT an exact 50/50
    split, which produces a near-exact tie that doesn't clearly demonstrate a real margin). For each
    input, run forwardPass(input, weights), print the input's label, the full probs vector, the
    winning class name (via CLASSES[argmax(probs)]), and the margin (top probability minus the
    second-highest probability). Add a numeric acceptance gate inside the script itself
    (process.exit(1) on failure) that operationalizes Success Criterion 2's qualitative wording as
    concrete thresholds derived from RESEARCH.md's own empirically observed 0.21-0.35 margin range:
    for each of the 4 canonical inputs, assert the winning class matches the expected domain-knowledge
    mapping (touch_hesitation->confusion, blur_incomplete->flow_friction, scroll_reversal->price_doubt,
    back_intent->trust_gap), assert the top probability is below 0.98 (not saturated toward ~1.0), and
    assert the margin is at least 0.05 (not collapsed toward the uniform ~0.25-each distribution).
    Print a final PASS/FAIL summary line and exit 0 only if all 4 canonical gates pass; the ambiguous
    input is printed for human inspection but is not gated to a specific winning class (genuine
    ambiguity is expected, not a bug).
  </action>
  <verify>
    <automated>node admin/print-softmax-margins.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node admin/print-softmax-margins.mjs` exits 0 and prints all 5 labeled softmax vectors (4 canonical + 1 ambiguous) with their margins.
    - Each of the 4 canonical inputs' printed winning class matches the domain-knowledge mapping exactly.
    - Each of the 4 canonical inputs' top probability is below 0.98 and its margin (top minus runner-up) is at least 0.05, enforced by the script's own exit-code gate, not just visual inspection.
    - The ambiguous input uses a 70/30 blend (`[0.7, 0.3, 0, 0]` or equivalent, not an exact 50/50 split).
  </acceptance_criteria>
  <done>Success Criterion 2 has its own standalone, numerically-gated verification artifact that prints and enforces real (non-saturated, non-uniform) softmax margins for all 4 canonical mappings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the softmax-margin phase gate and full test suite (Phase 3 gate, Success Criterion 2)</name>
  <action>Pause for human verification — the operator regenerates cold-start weights, runs the margin-printing script, reads the printed vectors, and confirms the full automated suite is green. No files are modified by this task; it is a blocking gate that closes Phase 3.</action>
  <what-built>
    The full inference pipeline wired end-to-end: signal:detected -> hand-written forward pass ->
    confidence gate -> inference:result (INF-01/02/03/05), a bounded session-end learning update
    (INF-04), and admin/print-softmax-margins.mjs's automated margin-quality gate for Success
    Criterion 2 already passing (Task 2).
  </what-built>
  <how-to-verify>
    1. Run `npm run generate-weights` followed by `node admin/print-softmax-margins.mjs` and read the
       printed output for all 5 vectors.
    2. Confirm each of the 4 canonical softmax vectors shows the correct class winning with a
       visually real margin (roughly 0.15-0.40 range) — not a saturated (~1.0/0.0) distribution and
       not a uniform (~0.25 each) distribution.
    3. Confirm the ambiguous 70/30 blended input's output looks like genuine, non-collapsed
       uncertainty (not identical to any single canonical vector's output).
    4. Run `npm test` (full suite) and confirm every test passes, including tests/inference.test.js
       and tests/inference-endsession.test.js.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/inference.js's import graph -> esbuild bundle -> dist/sdk.js | the final packaging boundary where an accidental brain.js import anywhere upstream would silently balloon the shipped partner-facing artifact |
| admin/print-softmax-margins.mjs (dev-only) -> printed console output | no network/persistence surface; pure read of committed weights + pure function calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | esbuild bundle graph (dist/sdk.js) | high | mitigate | Explicit grep-based build-output check (Task 1, via admin/check-bundle-purity.mjs) confirms brain.js's ~1MB UMD internals (NeuralNetworkGPU/thaw strings) never leak into the shipped bundle despite inference.js's cold-start weight loading being wired into index.js's import graph |
| T-03-10 | Denial of Service | Success Criterion 2's margin gate silently passing on a degenerate (dead-ReLU or saturated) weight set | medium | mitigate | admin/print-softmax-margins.mjs enforces numeric thresholds (top prob < 0.98, margin >= 0.05) with a nonzero exit code on failure, not just a printed value a human might skim past — catches the exact degeneracy modes RESEARCH.md's Pitfalls #1/#2 documented before the phase gate closes |
</threat_model>

<verification>
- `npm run build` succeeds; `dist/sdk.js` contains no brain.js internals.
- `node admin/print-softmax-margins.mjs` exits 0 with all 4 canonical margins passing the numeric gate.
- `npm test` (full suite, all phases) is green.
- Human-verify checkpoint confirms the printed softmax vectors read as genuinely non-saturated and non-uniform.
</verification>

<success_criteria>
The inference layer is reachable end-to-end from a real init() call, the shipped bundle is free of
brain.js internals, and Success Criterion 2's margin-quality requirement is enforced by both an
automated numeric gate and a human visual confirmation before the phase closes.
</success_criteria>

<output>
Create `.planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-05-SUMMARY.md` when done
</output>
