---
phase: 03-inference-layer-forward-pass-confidence-gate-cold-start-weig
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/inference.test.js
  - tests/inference-endsession.test.js
autonomous: true
requirements: [INF-01, INF-02, INF-03, INF-04, INF-05]
must_haves:
  truths:
    - "tests/inference.test.js encodes INF-01, INF-02, INF-03, INF-05 as executable assertions"
    - "tests/inference-endsession.test.js encodes INF-04 as executable state-diffing assertions"
    - "Both suites are RED at authoring time (src/inference.js does not exist yet) and fail to resolve"
  artifacts:
    - tests/inference.test.js
    - tests/inference-endsession.test.js
  key_links:
    - "describe blocks are keyed by requirement ID (INF-0N) so -t filters in the Validation Test Map resolve"
    - "Tests import only src/inference.js's API (no direct admin/weights.js import) so they stay independent of plan 03-01"
---

<objective>
Author the RED (Wave-0) unit-test suites that encode all five inference requirements as executable
assertions before any implementation exists, mirroring the Phase 1/2 precedent of a Wave-0 plan that
authors failing tests which later implementation flips green.

Purpose: locks the behavioral contract for INF-01 through INF-05 up front so plans 03-03 and 03-04
implement against fixed assertions rather than interpreting prose.
Output: tests/inference.test.js (INF-01/02/03/05), tests/inference-endsession.test.js (INF-04), both RED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-CONTEXT.md
@.planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-RESEARCH.md
@.planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-PATTERNS.md
@tests/signal.test.js
@tests/fixtures/test-subscriber.js
</context>

## Artifacts this phase produces (this plan's slice)

- New file `tests/inference.test.js` — describe blocks `INF-01`, `INF-02`, `INF-03`, `INF-05`.
- New file `tests/inference-endsession.test.js` — describe block `INF-04`.

<tasks>

<task type="auto">
  <name>Task 1: Author tests/inference.test.js (RED) — INF-01, INF-02, INF-03, INF-05</name>
  <files>tests/inference.test.js</files>
  <read_first>
    - tests/signal.test.js (RED-suite header convention lines 1-9; describe-per-requirement-ID + fixture pattern lines 42-60; collectReceived subscriber usage)
    - tests/fixtures/test-subscriber.js (collectReceived helper for capturing bus events)
    - .planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-RESEARCH.md ("Phase Requirements → Test Map"; "Hand-Written Forward Pass" and "Confidence Gate & Bus Publish" patterns; Assumption A2 for the ambiguous stress input)
    - .planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-PATTERNS.md (tests/inference.test.js analog section)
  </read_first>
  <action>
    Create tests/inference.test.js importing the not-yet-existent named exports from `../src/inference.js`
    (`forwardPass`, `relu`, `softmax`, `initInference`) plus `subscribe` from `../src/bus.js` and
    `collectReceived` from `./fixtures/test-subscriber.js`. Add a file header comment stating it is a
    RED Wave-0 suite covering INF-01/02/03/05 that fails to resolve until src/inference.js exists.
    Structure with `describe('INF-01', ...)`, `describe('INF-02', ...)`, `describe('INF-03', ...)`,
    `describe('INF-05', ...)` so `-t "INF-0N"` filters resolve. Assertions:
    INF-01 — feeding any valid 4-element input through `forwardPass(input, fixtureWeights)` returns a
    `probs` array of length 4 whose entries sum to 1 (within a small epsilon) and are each in [0,1].
    INF-02 — for a hand-authored fixture `{W1,b1,W2,b2}`, assert `forwardPass`'s `probs`/`logits`/`hidden`
    equal values you compute by hand from the explicit W1/b1→ReLU→W2/b2→softmax arithmetic for a chosen
    input — proving the forward pass is genuinely explicit, not a wrapped brain.js call. Use a fixture
    weights object defined IN the test (do NOT import admin/weights.js) so this suite stays independent
    of plan 03-01.
    INF-03 — construct fixture weights that yield a max softmax probability below 0.65 for a given
    input and assert the published `inference:result.fires` is false; construct another yielding >=0.65
    and assert `fires` is true. Drive this by subscribing to `inference:result`, calling
    `initInference({ inference: { weights: fixtureWeights, confidenceThreshold: 0.65 } })`, then
    `publish('signal:detected', { type: 'touch_hesitation', ... })`.
    INF-05 — assert `initInference` with `config.inference.weights` present uses the injected weights
    (a signal produces the probs those weights imply), and that calling `initInference` with no
    `config.inference.weights` still yields a valid distribution summing to 1 (the bundled cold-start
    default) — without importing admin/weights.js directly.
    Also assert D-01 shape: every `signal:detected` yields exactly one `inference:result` carrying
    `intent`, `confidence`, `probs`, and `fires`.
  </action>
  <verify>
    <automated>npx vitest run tests/inference.test.js 2>&1 | grep -Eiq "fail|error|cannot find|does not provide" && echo "RED as expected" || (echo "expected RED failure" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - tests/inference.test.js contains `describe('INF-01'`, `describe('INF-02'`, `describe('INF-03'`, `describe('INF-05'` blocks (source assertion).
    - The suite is RED: `npx vitest run tests/inference.test.js` fails (module `../src/inference.js` does not resolve yet) — this is the intended Wave-0 state.
    - INF-02's assertion uses an in-file fixture weights object, not an import of admin/weights.js (grep: no `admin/weights` reference in the file).
    - The suite asserts probs sum to ~1 (INF-01), explicit-arithmetic equality (INF-02), fires-flag gating at 0.65 (INF-03), and injected-vs-default weight selection (INF-05).
  </acceptance_criteria>
  <done>A RED unit suite exists encoding INF-01/02/03/05 as concrete assertions, independent of plan 03-01, ready for plan 03-03 to turn green.</done>
</task>

<task type="auto">
  <name>Task 2: Author tests/inference-endsession.test.js (RED) — INF-04</name>
  <files>tests/inference-endsession.test.js</files>
  <read_first>
    - tests/signal.test.js (before/after state-diffing style; describe-per-requirement-ID header convention)
    - .planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-RESEARCH.md ("Session-End Weight Update" pattern 4; "Verified train() Single-Step Semantics"; Test Map INF-04 row; Assumption A1 buildTarget rule)
    - .planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-PATTERNS.md (tests/inference-endsession.test.js analog section; state-diffing convention)
  </read_first>
  <action>
    Create tests/inference-endsession.test.js importing `initInference`, `endSession`, and `forwardPass`
    from `../src/inference.js` and `publish`/`subscribe` from `../src/bus.js`. Add a RED-suite header
    comment noting it covers INF-04, kept in a separate file per Phase-2's isolate-distinct-concerns
    precedent, and fails to resolve until src/inference.js exists. Under `describe('INF-04', ...)`
    assert the session-end weight-update semantics (D-03): (1) publish one `signal:detected`, then call
    `endSession(config, false)` once, and assert the active weights CHANGED — probe by re-running an
    inference on the same signal type and observing the published `probs` differ from the pre-endSession
    probs; (2) call `endSession(config, false)` a SECOND time and assert probs change AGAIN (proving each
    call is a genuine independent gradient step, not a no-op); (3) assert that merely publishing multiple
    `signal:detected` events with NO endSession call does NOT change the weights (proving the update fires
    at session end only, never per-event — INF-04's core guarantee); (4) assert `endSession` before any
    signal has fired this session is a safe no-op (lastInference null path). Use synthetic boolean
    outcomes (true=flowComplete, false=abandoned) passed directly to endSession — no live flow, no DOM.
  </action>
  <verify>
    <automated>npx vitest run tests/inference-endsession.test.js 2>&1 | grep -Eiq "fail|error|cannot find|does not provide" && echo "RED as expected" || (echo "expected RED failure" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - tests/inference-endsession.test.js contains a `describe('INF-04'` block (source assertion).
    - The suite is RED: it fails because `../src/inference.js` does not resolve yet (intended Wave-0 state).
    - Assertions cover: weights change after one endSession, change again after a second, do NOT change across per-event signal:detected without endSession, and endSession is a safe no-op before any signal.
    - Outcomes are passed as synthetic booleans directly to endSession (no live flow / DOM dependency).
  </acceptance_criteria>
  <done>A RED unit suite exists encoding INF-04's exactly-once-per-session bounded-update contract, ready for plan 03-04 to turn green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → src/inference.js API | tests exercise the module's public surface only; no external input surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Repudiation | test coverage gaps | low | mitigate | Every requirement ID (INF-01..05) gets a keyed describe block matching the Validation Test Map, so a missing assertion is visible as an unmapped requirement |
| T-03-SC | Tampering | npm installs | low | accept | No package installs in this plan; brain.js install is confined to plan 03-01 where the legitimacy audit applies |
</threat_model>

<verification>
- Both test files exist with requirement-ID-keyed describe blocks.
- Both suites are RED (fail to resolve on missing src/inference.js) — the correct Wave-0 state.
- Neither suite imports admin/weights.js (independence from plan 03-01 preserved).
</verification>

<success_criteria>
RED unit suites for INF-01 through INF-05 exist as executable assertions and fail cleanly for the
right reason (no implementation yet), locking the behavioral contract for the implementation waves.
</success_criteria>

<output>
Create `.planning/phases/03-inference-layer-forward-pass-confidence-gate-cold-start-weig/03-02-SUMMARY.md` when done
</output>
